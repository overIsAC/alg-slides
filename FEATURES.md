# 算法课件演示站 · 功能清单

面向使用者的说明：这个网站**能做什么、怎么用**。
（技术实现、构建与发布流程不在此文件，见文末「维护入口」。）

- 线上地址：<https://overisac.github.io/alg-slides/>
- 内容规模：370 篇笔记 · 4 份课件（快速幂 14 页、光速幂 10 页、中位数定理 6 页、前缀和 7 页）
- 离线可用：数学公式（KaTeX）、笔记图片均已本地化，断网也能正常显示

---

## 一、左侧 · 目录区

**首页**：目录最上方固定一项「🏠 首页」，点进去是站点简介 —— 这个项目是什么、想法怎么来的。
**第一次访问**（本机还没有浏览记录）打开站点就直接是它。

### 1. 多级文件树
按 Obsidian 笔记库 `Code\lecture` 的原始目录结构展示，可逐级点开/收起（点击文件夹一行）。
文件夹右侧数字 = 该目录下笔记篇数。

### 2. 课件 / 笔记 标记
每个文件行末尾有标签：

| 标签 | 含义 | 点进去看到什么 |
|---|---|---|
| **课件**（带星） | 有专门制作的 PPT 式翻页课件 | 一屏一页，可翻页演示 |
| **笔记** | 暂无课件 | 该 md 的原文渲染（长文滚动阅读） |

> `100-基础算法\80-乘方\` 下的「10-快速幂」「20-光速幂」都已做成课件，均标**课件**；
> `100-基础算法\30-贪心\` 下的「50-中位数定理」也已做成课件，标**课件**；
> `100-基础算法\40-前缀和与差分\` 下的「10-前缀和」也已做成课件，标**课件**；
> 其余目录的笔记暂未做课件，标**笔记**。

### 3. 搜索框
输入即筛选，实时过滤文件树。
匹配范围：**文件名 + 完整路径 + frontmatter 里的 `aliases` 别名**（不搜正文）。
例如搜「幂」会同时命中快速幂、光速幂；搜别名也能找到对应笔记。

### 4. 底部状态栏
- 左：`370 篇 · 3 个课件`（全站统计）
- 右：快捷键提示 `←` `→` 翻页

---

## 二、右侧 · 主区域

### 5. 顶部工具栏

| 元素 | 作用 |
|---|---|
| 面包屑 | 显示当前笔记所在目录 + 文件名 |
| 🎞 课件 / 📄 笔记 | 两种阅读模式切换（**只有该篇做了课件时「课件」按钮才可用**） |
| 页数徽标 | 课件模式下显示总页数，如「课件 14 页」 |
| ⛶ | 全屏 / 退出全屏（快捷键 `F`） |
| ☰ | 收起 / 展开左侧目录（快捷键 `B`），适合投屏演示 |
| by acidlemon | 右下角署名，点击跳转作者洛谷主页 |

### 6. 课件模式（PPT 式翻页）
- **一屏一页**，内容按页精排：问题卡、公式推导、代码、对照表、动画、习题表等
- **顶部进度条**显示当前进度
- **底部分页器**：`«` `‹` `›` `»` —— 跳到第一页 / 上一页 / 下一页 / 最后一页；中间页码**可点击后输入数字直接跳转**
- **数学公式**用 LaTeX 渲染（KaTeX）
- **交互动画**：部分页面带可操作的演示（见下节）
- **输入校验提示**：动画里的输入框（底数/指数）如果留空、填了小数/字母、或超出允许范围，
  点「自动演示 / 单步」时会在页面上方弹出一条提示，约 2 秒后自动消失，不打断演示
- **数学习题表难度徽标**：难度写「橙题 / 黄题 / 蓝题 / 紫题」等洛谷难度名；AtCoder 的题写成「533/黄题」时，
  会自动拆成两块圆角矩形 —— 评分按 AtCoder Problems 的分档上色（灰 <400、棕 400、绿 800、青 1200、蓝 1600、黄 2000、橙 2400、红 2800+），
  难度按洛谷色上色；非 AtCoder 的题保持原样。
  Codeforces 的题写成「1400/橙题」时，评分**不自动上色**（rating 需手写在课件里），色带沿用同一张表按 CF rating 分档
  （不到 800 灰、800 绿、1200 青、1600 蓝、2000 黄、2400 橙、2800+ 红）
- **刷新后回到原处**：当前看到哪篇笔记、课件还是笔记模式、课件第几页，会记在本机浏览器里；
  按 F5 或关掉重开，会直接回到上次那个位置（**第一次访问 / 清了浏览器数据**则进首页）
- 课件模式与笔记模式可随时来回切换，切换后仍停在同一篇

### 7. 笔记模式（原文阅读）
把 md 原文渲染成网页：标题层级、表格、代码块高亮、图片、行内与独立公式。
内容即 Obsidian 里的原文，不做删改。

### 8. 笔记正文里的内链跳转
正文中的 Obsidian 双链 `[[...]]` 渲染成**可点击的站内链接**（虚线下划线样式），
点击直接跳到对应笔记；带 `#标题` 的链接会**自动滚动到该标题位置**。
链接文本按显示别名优先，例如 `[[170-图论/10-概念\|图论概念]]` 显示为「图论概念」。

### 9. 图片显示
笔记中的 `![[图片]]` 与 `![](...)` 均从站内 `assets/attachments/` 加载，
**不依赖本机路径，断网/换机器都能显示**。

---

## 三、交互动画（课件内）

| 课件 | 页面 | 动画 |
|---|---|---|
| 快速幂 | 第 2 页 · 递归写法 | **递归拆解演示**：输入底数和指数，看递归如何一层层折半展开 |
| 快速幂 | 第 5 页 · 循环写法 | **两两合并演示**：把 n 个底数逐轮两两合并的全过程 |
| 光速幂 | 第 6 页 · 演示：预处理出来的表 | **分块定位演示**：输入 B1 / B2 / 指数 b，表格行 = f1、列 = f2，命中格高亮其余淡显 |
| 中位数定理 | 第 4 页 · 最小总距离 | **数轴拖动演示**：输入 n 和 n 个整数位置（或用预设），数轴上标出这些位置与选择的位置 x；拖动 x 只在整数上跳，实时显示「选择的 x = …，总距离 …」及 $\lvert x-a_i \rvert$ 的求和式；轴上还会标出总距离最小的位置（奇数一个圈、偶数一段线段） |
| 前缀和 | 第 4 页 · 前缀和数组的生成与查询 | **生成 / 查询演示**：上半输入 n 与数组 a，立即算出 pre 数组（额外显示 pre[0]）；点「▶ 演示 pre 数组的生成过程」后一步步高亮 pre[i-1]（蓝）、a[i]（橙）、pre[i]（绿），并给出 `pre[i] = pre[i-1] + a[i]` 的算式。下半输入 l、r 后高亮 pre[l-1]（蓝）与 pre[r]（绿），给出 `pre[r] - pre[l-1]` 的减法式与区间和结果 |

---

## 四、键盘快捷键

| 按键 | 作用 |
|---|---|
| `→` `↓` `空格` `PageDown` | 下一页 |
| `←` `↑` `PageUp` | 上一页 |
| `Home` / `End` | 跳到第一页 / 最后一页 |
| `F` | 全屏切换 |
| `B` | 收起 / 展开左侧目录 |
| `N` | 课件 ↔ 笔记 切换 |

> 在输入框（搜索框、页码输入）内打字时，快捷键不生效，避免误触。

---

## 五、已有课件

| 笔记路径 | 课件名 | 页数 |
|---|---|---|
| `100-基础算法/80-乘方/10-快速幂` | 快速幂 | 14 |
| `100-基础算法/80-乘方/20-光速幂` | 光速幂 | 10 |
| `100-基础算法/30-贪心/50-中位数定理` | 中位数定理 | 6 |
| `100-基础算法/40-前缀和与差分/10-前缀和` | 前缀和 | 7 |

---

## 六、维护入口（给维护者看，非使用者功能）

| 想做的事 | 入口 |
|---|---|
| 改完 md 后重建 + 发布上线 | 双击 `deploy-github.bat` |
| 只重建本地、不发布 | 双击 `rebuild.bat` |
| 新增/修改课件 | 编辑 `data/decks/<英文名>.js`，并在 `index.html` 加一行 script |
| 新增/修改课件里的演示 | 演示放 `assets/decks/<课件>/demo-xxx.js`、配套样式 `assets/decks/<课件>/<课件>.css`，两者都要在 `index.html` 引入 |
| 发布时压缩混淆 | `build/minify.cjs`（terser 压 JS + clean-css 压 CSS + html-minifier 压 HTML），`deploy-github.bat` 打包到上传仓库后自动运行；只压缩上传副本，源码保持可调试 |
| 课件编写规范 | skill「制作课件约束」 |

> 网站是**纯静态产物**：运行时只读 `data/notes.js` 和 `data/decks/`，
> 不会去读本机的 md 文件。改动 md 后必须重建才会反映到网站上。
>
> 两种模式的数据来源不同：**笔记模式**来自 `data/notes.js`，改 md + 重建就自动更新；
> **课件模式**来自手写的 `data/decks/<英文名>.js`，改 md **不会**自动同步过去，要手动改课件里的页面。

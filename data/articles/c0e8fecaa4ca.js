/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["170-图论/20-存图/60-邻接表之链式前向星"]="<h1>前言</h1>\n<p>旧时代的产物。</p>\n\n<p>现在无论是<code>csp-j/s</code>，还是<code>noip</code>都有<code>O2</code>优化，<code>vector</code>的性能不输链式前向星，并且链式前向星还很难理解，写得相对复杂。因此邻接表可以只学<code>vector</code>。</p>\n\n<p>在写网络流候，用链式前向星存图，使用异或$1$的方式找到反向边。<br />\n不过<code>vector</code>也可以找到，需要多存一个下标。</p>\n\n<h1>实现</h1>\n\n<p>$head_x$表示节点$x$第一条邻边的下标。<br />\n$Edge$表示边，其中$next$则是$x$的下一条边的下标。</p>\n\n<p>初始化：<br />\n①<code>head</code>初始化为$0$；<br />\n②<code>id</code>初始化为$0$。如果是网络流，则初始化为$1$。</p>\n\n\n<pre><code class=\"language-cpp\">struct Edge {\n    int y, w;\n    int next;\n} E[N]; // 边数，如果是无向边，注意开两倍\n\nint head[N]; // 点数\nint id;  // 共id条边，1到id表示边的下标\n\nvoid add(int x, int y, int w) {\n    E[++id].y = y;\n    E[id].w = w;\n    E[id].next = head[x];\n    head[x] = id;\n}\n\nint main() {\n    memset(head, 0, sizeof(head));\n    id = 0;\n\n    int n, m;\n    cin &gt;&gt; n &gt;&gt; m;\n\n    while (m--) {\n        int u, v, w;\n        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;\n\n        add(u, v, w);\n        add(v, u, w);\n    }\n\n    return 0;\n}\n\n</code></pre>\n\n<h1>遍历</h1>\n\n\n<pre><code class=\"language-cpp\">int x = 1;\nfor (int i = head[x]; i; i = E[i].next) {\n        int y = E[i].y, w = E[i].w;\n        // 遍历x的所有边\n        // x到y的边，边权为w\n}\n</code></pre>\n";

/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["杂项/标准库/rand函数"]="<p>这是一个生成随机数的函数。</p>\n\n<h1>使用</h1>\n\n<p>在使用之前需要调用一遍<code>srand(time(NULL))</code>。如果不使用，则每次运行获得的随机数是一样的。<br />\n其中<code>time(NULL)</code>代表当前时间，作为时间种子。<code>time(NULL)</code>每一秒变化一次，获得的随机数也会随之不同。</p>\n\n<p>使用<code>rand()</code>会获得$0$到<code>RAND_MAX</code>范围内的整数。（<code>RAND_MAX</code>在后面说明）</p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    srand(time(NULL));\n    int n = rand();\n    cout &lt;&lt; n;\n    return 0;\n}\n</code></pre>\n\n<h1>生成指定范围内的随机数</h1>\n\n<p>①使用取模将随机数限定到$0$到$r-l$。<br />\n②加上$l$将范围变成了$l$到$r$。</p>\n<pre><code class=\"language-cpp\">int f(int l, int r) {\n    return rand() % (r - l + 1) + l;\n}\n</code></pre>\n\n<h1>生成一个数组</h1>\n\n<p>①生成一个长度$n$。<br />\n②再循环生成$n$个数字。</p>\n<pre><code class=\"language-cpp\">int n, a[100005];\n\nn = f(1, 10);\nfor (int i = 1; i &lt;= n; ++i) {\n    a[i] = f(1, 5);\n}\n</code></pre>\n\n<h1>生成一个字符</h1>\n\n<p>直接填想要字符的<code>ASCII</code>就行了。</p>\n\n<p>比如填小写字符的范围。</p>\n<pre><code class=\"language-cpp\">char ch = f('a', 'z');\n</code></pre>\n\n<h1>生成一个字符串</h1>\n\n<p>①生成一个长度$n$。<br />\n②再循环生成$n$个字符。</p>\n\n\n<pre><code class=\"language-cpp\">int n;\nstring s;\n\nn = f(1, 10);\nfor (int i = 1; i &lt;= n; ++i) {\n    s += f('a', 'z');\n}\ncout &lt;&lt; s;\n</code></pre>\n\n<h1>注意</h1>\n\n<p>①<code>rand</code>函数在<code>Windows</code>的最大值是 $32767$，<code>Linux</code>的最大值是$2^{31}-1$，可以通过<code>RAND_MAX</code>查看。<code>CCF</code>使用的系统是<code>NOI Linux</code>，但在本地测试时注意最大范围。</p>\n\n<p>看<code>RAND_MAX</code>。</p>\n<pre><code class=\"language-cpp\">cout &lt;&lt; RAND_MAX &lt;&lt; endl;\n</code></pre>\n\n<p>②随机数弱，不够均匀。</p>\n";

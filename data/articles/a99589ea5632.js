/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["10-语法/330-结构体"]="<h1>前言</h1>\n\n<p><code>C++</code>有<code>int</code>、<code>long long</code>、<code>bool</code>、<code>char</code>等等这些基本类型。</p>\n\n<p>可以使用<code>struct</code>这一个关键字定义自定义类型。</p>\n<h1>代码</h1>\n\n<p>代码格式，注意最后需要一个分号，没有分号会报错。</p>\n<pre><code>struct 名字 {\n    定义成员...\n};\n</code></pre>\n\n<p>定义一个学生类型。里面的变量叫做成员变量。</p>\n\n\n<pre><code class=\"language-cpp\">struct Student {\n    int id;\n    char name[20];\n    int sex;\n    int age;\n};\n</code></pre>\n\n<p>定义之后，$Student$就是一个新的类型。<br />\n可以用 $Student$ 定义变量、数组.....</p>\n\n<p>定义出来的变量，可以叫做结构体变量。<br />\n要想使用结构体变量里面的变量，需要使用<code>.</code>运算符。</p>\n<pre><code class=\"language-cpp\">int main() {\n    Student a;\n    a.id = 1;\n    strcpy(a.name, &quot;小红&quot;);\n    a.sex = 0;\n    a.age = 18;\n\n    cout &lt;&lt; a.id &lt;&lt; endl;\n    cout &lt;&lt; a.name &lt;&lt; endl;\n    cout &lt;&lt; a.sex &lt;&lt; endl;\n    cout &lt;&lt; a.age &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n\n<p>里面的变量的用法和普通变量一样。</p>\n\n<p>通过<code>struct</code>关键字，可以将几个变量绑在一起，形成一个新的类型。</p>\n\n<h1>花括号赋值</h1>\n\n<p>上面使用了赋值方式是一个一个赋值。</p>\n\n<p>还一种方式，使用花括号赋值。<br />\n在花括号里面的值，会按照结构体里面的变量的顺序，对应赋值。</p>\n\n\n<pre><code class=\"language-cpp\">struct Student {\n    int id;\n    char name[20];\n    int sex;\n    int age;\n};\n\nint main() {\n    Student a = {1, &quot;hong&quot;, 1, 18}; \n\n    cout &lt;&lt; a.id &lt;&lt; endl;\n    cout &lt;&lt; a.name &lt;&lt; endl;\n    cout &lt;&lt; a.sex &lt;&lt; endl;\n    cout &lt;&lt; a.age &lt;&lt; endl;\n    cout &lt;&lt; &quot;------------&quot; &lt;&lt; endl;\n\n    // 如果不是初始化，在dev里面就需要强制类型转换 \n    a = (Student){2, &quot;hua&quot;, 0, 22}; \n    cout &lt;&lt; a.id &lt;&lt; endl;\n    cout &lt;&lt; a.name &lt;&lt; endl;\n    cout &lt;&lt; a.sex &lt;&lt; endl;\n    cout &lt;&lt; a.age &lt;&lt; endl;\n    return 0;\n}\n</code></pre>\n\n<h1>结构体数组</h1>\n<p>像普通类型一样，也可以定义数组：</p>\n<pre><code class=\"language-cpp\">struct Student {\n    int id;\n    char name[20];\n    int sex;\n    int age;\n};\n\nint main() {\n    Student a[100];\n\n    a[1] = (Student){11, &quot;Zhang San&quot;, 1, 18};\n    a[2] = (Student){12, &quot;Li Si&quot;, 0, 22};\n    a[3] = (Student){13, &quot;Wang Wu&quot;, 1, 19};\n\n    return 0;\n}\n</code></pre>\n\n<h1>函数传参</h1>\n\n<p>函数传参和普通变量一样。</p>\n\n\n<pre><code class=\"language-cpp\">struct Student {\n    int id;\n    char name[20];\n    int sex;\n    int age;\n};\n\nvoid f(Student stu) {\n    cout &lt;&lt; &quot;名字叫做&quot; &lt;&lt; stu.name &lt;&lt; endl;\n}\n\nint main() {\n    Student a[100];\n\n    a[1] = (Student){11, &quot;Zhang San&quot;, 1, 18};\n    a[2] = (Student){12, &quot;Li Si&quot;, 0, 22};\n    a[3] = (Student){13, &quot;Wang Wu&quot;, 1, 19};\n\n    f(a[1]);\n    f(a[3]);\n\n    return 0;\n}\n</code></pre>\n\n<h1>结构体排序</h1>\n<p>结构体排序可以<code>std::sort</code>函数排序，但是需要使用<code>cmp</code>函数。</p>\n\n<p><code>cmp</code>函数的编写规则和普通类型是一样的。只是将两个参数换成两个结构体。</p>\n\n<p>例如下面代码按照年龄从小到大排序：</p>\n\n\n<pre><code class=\"language-cpp\">struct Student {\n    int id;\n    char name[20];\n    int sex;\n    int age;\n};\n\n\nint cmp(Student x, Student y) {\n    return x.age &lt; y.age;\n}\n\nint main() {\n    Student a[100];\n\n    a[1] = (Student){11, &quot;ZhangSan&quot;, 1, 18};\n    a[2] = (Student){12, &quot;LiSi&quot;, 0, 22};\n    a[3] = (Student){13, &quot;WangWu&quot;, 1, 19};\n\n    sort(a + 1, a + 3 + 1, cmp);\n    for (int i = 1; i &lt;= 3; ++i) {\n        cout &lt;&lt; a[i].id &lt;&lt; &quot; &quot; &lt;&lt; a[i].name &lt;&lt; &quot; &quot; &lt;&lt; a[i].sex &lt;&lt; &quot; &quot; &lt;&lt; a[i].age &lt;&lt; endl;\n    }\n\n    return 0;\n}\n</code></pre>\n\n<h1>成员函数</h1>\n<p>to be continue</p>\n<h1>重载运算符</h1>\n\n<p>定义出来的结构体，大部分运算符是使用不了的，例如：<code>+</code>、<code>-</code>、<code>&lt;</code>、<code>&gt;</code>等等。</p>\n\n<p>如果想要使用这些运算符，就得写函数来实现，这个函数是特定的格式。<br />\n写出的函数，也可以叫做重载运算符。</p>\n<h2>小于号</h2>\n<p>小于号相较于其它符号，是最常用，以小于号为例。</p>\n\n<h3>方式1</h3>\n<pre><code class=\"language-cpp\">struct Student {\n    int age, score;\n};\n\n\nint operator&lt; (Student x, Student y) {\n    // ①小于号是两个变量比较，所以参数有两个\n    // ②第1个参数是小于号左边的变量，第2个参数是小于号右边的变量\n    // ③int是返回值，operator&lt;代表小于号。如果写operator&gt;就代表大于号\n\n    // 该代码含义是：如果x的age小于y的age，那么x小于y\n    return x.age &lt; y.age;\n}\n\nint main() {\n    Student a, b;\n\n    a.age = 18;\n    b.age = 19;\n\n    if (a &lt; b) {\n        cout &lt;&lt; &quot;a更小&quot;;\n    } else {\n        cout &lt;&lt; &quot;b更小&quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n<h2>方式2</h2>\n<p>to be continue</p>";

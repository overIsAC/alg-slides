/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["杂项/指针"]="<h1>前言</h1>\n\n<p>指针比较抽象，比较难。<br />\n要求，大部分了解就行了，暂时不用管指针需要用在哪里。</p>\n\n<h1>内存大小</h1>\n\n<table>\n<thead>\n<tr>\n<th>类型</th>\n<th>内存大小（字节）</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>char</code></td>\n<td>1</td>\n</tr>\n<tr>\n<td><code>bool</code></td>\n<td>1</td>\n</tr>\n<tr>\n<td><code>short</code></td>\n<td>2</td>\n</tr>\n<tr>\n<td><code>int</code></td>\n<td>4</td>\n</tr>\n<tr>\n<td><code>long</code></td>\n<td>4</td>\n</tr>\n<tr>\n<td><code>long long</code></td>\n<td>8</td>\n</tr>\n<tr>\n<td><code>float</code></td>\n<td>4</td>\n</tr>\n<tr>\n<td><code>double</code></td>\n<td>8</td>\n</tr>\n</tbody>\n</table>\n\n<h1>内存</h1>\n\n<p>代码运行起来后，变量都存放在里面。<br />\n如下图，一个格子代表<code>1</code>个字节，也就是<code>8</code>比特。<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/aa251b_Pasted image 20240928000044.png\" /><br />\n如果是一个<code>char</code>类型变量，占用<code>1</code>个字节；如果是一个<code>int</code>类型变量，就占用<code>4</code>个字节......<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/ac6010_Pasted image 20240928000700.png\" /></p>\n\n<h1>地址</h1>\n<p>每个内存都有一个编号，这个编号叫做地址。<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/7170cf_Pasted image 20240928001552.png\" /></p>\n<h2>取地址运算符</h2>\n\n<p><code>&amp;</code>符号</p>\n<pre><code class=\"language-cpp\">int main() {\n    int a = 1;\n    cout &lt;&lt; &amp;a &lt;&lt; endl; // 这里的&amp;表示取地址，会输出一个十六进制数字\n    return 0;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:2em\"></div>\n<h1>指针概念</h1>\n\n<p>指针就是存储着这个编号。<br />\n指针就是存着地址的变量。</p>\n\n<p>例如下图：<br />\n指针 <code>p1</code> 存着 $124$ 这个地址。<br />\n指针 <code>p2</code> 存着 $126$ 这个地址。<br />\n我们就可以通过指针找到地址对应的内存。<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/78e9bd_Pasted image 20240928002555.png\" /></p>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<p><code>指针 -&gt; 地址 -&gt; 对应的内存 -&gt; 内存存的数字</code></p>\n\n<h2>定义指针</h2>\n\n<p>指针是有类型的。</p>\n\n<p>注意：①要么把变量的地址赋值给指针；②要么把指针存的地址赋值给另一个指针。</p>\n<pre><code class=\"language-cpp\">int main() {\n    int *p; // 定义一个int类型的指针\n    int a = 1;\n    cout &lt;&lt; &amp;a &lt;&lt; endl;\n\n    p = &amp;a;  // 把a的地址存到了指针p里面，可以说p指向a\n    cout &lt;&lt; p &lt;&lt; endl;  // a的地址\n    cout &lt;&lt; *p &lt;&lt; endl; // 找到变量a的值\n\n    double *p2 = &amp;a; // 编译错误，类型不对\n\n    return 0;\n}\n</code></pre>\n\n<h2>空指针</h2>\n<pre><code class=\"language-cpp\">int main() {\n    int *p1; // 不赋值，p1存的地址是乱的，称p1是野指针\n    int *p2 = NULL; // NULL是编号是0的指针。NULL就是0。也可以称NULL是 空\n\n    return 0;\n}\n</code></pre>\n\n<h2>易错</h2>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    int *p1, p2; // p1是int指针。p2是普通的int，不是指针\n\n    int* p1, p2; // 容易造成误导，以为p2也是指针。实际和上面一样。\n\n    return 0;\n}\n</code></pre>\n\n<h2>寻址运算符</h2>\n<p>通过指针，找到内存存的数字。</p>\n\n<p><code>*</code> 符号</p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    int *p; // 定义一个int类型的指针\n    int a = 1;\n    p = &amp;a;  // 把a的地址存到了指针p里面，可以说p指向a\n    cout &lt;&lt; *p &lt;&lt; endl; // 找到变量a的值\n\n\n    cout &lt;&lt; *p + 2 &lt;&lt; endl; // 运算\n    *p = 100; //赋值\n    cout &lt;&lt; *p &lt;&lt; endl; // 修改\n\n\n    return 0;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<h1>指针运算</h1>\n<h2>指针移动</h2>\n\n<p>可以使用 <code>++</code> 这个运算符让指针往后移动。<br />\n移动多少取决于是什么类型，这个类型内存大小是多少，就移动多少。</p>\n\n<p>如果是 <code>char</code> 类型，就移动<code>1</code>格。<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/70ede8_Pasted image 20240928003700.png\" /><br />\n如果是 <code>int</code> 类型，就移动<code>4</code>格。<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/227b3e_Pasted image 20240928003922.png\" /></p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    int *p; // 定义一个int类型的指针\n    int a = 1;\n    p = &amp;a;  // 把a的地址存到了指针p里面，可以说p指向a\n\n    cout &lt;&lt; p &lt;&lt; endl;\n    ++p; // 地址 往后跳了4个字节\n    cout &lt;&lt; p &lt;&lt; endl;\n\n    long long b;\n    long long *p2 = &amp;b;\n    cout &lt;&lt; p2 &lt;&lt; endl;\n    ++p2; // 地址 往后跳了8个字节\n    cout &lt;&lt; p2 &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n\n<h2>指针比较</h2>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    int *p1 = NULL;\n    int *p2;\n\n    if (p1 == p2) {\n        // 如果相同地址\n    }\n\n    if (p1 != p2) {\n        // 如果不同地址\n\n    }\n\n    if (p1 == NULL) {\n\n    }\n\n    if (p1 != NULL) {\n\n    }\n\n    return 0;\n}\n</code></pre>\n\n<h1>指针和数组</h1>\n\n<p>数组名存的就是数组第一个元素的地址。</p>\n<pre><code class=\"language-cpp\">int main() {\n\n    int a[10];\n\n    cout &lt;&lt; &amp;a[0] &lt;&lt; endl;\n    cout &lt;&lt; a &lt;&lt; endl; \n\n    return 0;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<p>指针可以当作数组使用。</p>\n<pre><code class=\"language-cpp\">int main() {\n\n    int a[10] = {1, 1, 4, 5, 1, 4};\n\n    int *p = a;\n\n    for (int i = 0; i &lt; 6; ++i) {\n        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n    }\n    cout &lt;&lt; endl;\n\n    for (int i = 0; i &lt; 6; ++i) {\n        cout &lt;&lt; p[i] &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<h2>课外思考</h2>\n\n<p>在64位的计算机上，不管什么类型的指针的内存大小都是8字节。<br />\n在32位的计算机上，不管什么类型的指针的内存大小都是4字节。</p>\n\n<p>①为什么指针要有类型呢？</p>\n\n<h1>函数传参</h1>\n\n<p>to be continue</p>";

/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["杂项/标准库/STL容器/string"]="<p>动态长度的字符串。<br />\n有一些比字符数组更方便、好用的函数。</p>\n\n<h1>定义</h1>\n\n<p>每个元素都是<code>char</code>类型。</p>\n<pre><code class=\"language-cpp\">string s1;          // 空串\nstring s2(5, 'a');  // &quot;aaaaa&quot;  5个a\nstring s3 = &quot;abc&quot;;  // 将字符串常量赋值给string类型\n\nchar s[10] = &quot;qwe&quot;;\nstring s4 = s;  // 将字符数组赋值给string类型\n</code></pre>\n<h1>输入输出</h1>\n\n<p>可以直接<code>cin</code>读入， <code>cout</code>输出。<br />\n使用和普通数组类似，但是<strong>注意不要越界</strong>。</p>\n\n\n<pre><code class=\"language-cpp\">\nstring s;\ncin &gt;&gt; s;  // 如果输入 hi\n\ncout &lt;&lt; s[0] &lt;&lt; endl;  // h\ncout &lt;&lt; s[1] &lt;&lt; endl;  // i\ncout &lt;&lt; s &lt;&lt; endl;     // hi   全部输出\n\ns[0] = 'a';\ns[1] = 'a';\ncout &lt;&lt; s &lt;&lt; endl;     // aa   全部输出\n\n// 错误，越界了\ncout &lt;&lt; s[2] &lt;&lt; endl;\ncout &lt;&lt; s[-1] &lt;&lt; endl;\n\n// 使用printf输出\nprintf(&quot;%s&quot;, s.c_str()); // 得到一个char*指针\n\n</code></pre>\n\n<h1>大小</h1>\n\n<p><code>size</code>函数，查询<code>string</code>大小。</p>\n\n\n<pre><code class=\"language-cpp\">string s;\ncin &gt;&gt; s;\ncout &lt;&lt; s &lt;&lt; endl;\n</code></pre>\n\n<h1>拼接</h1>\n\n<p>可以将 <code>string</code> 拼接起来。</p>\n<pre><code class=\"language-cpp\">string s = &quot;hi&quot;;\nstring t = &quot;lihua&quot;;\ncout &lt;&lt; s + t &lt;&lt; endl;\n\nstring z = s + t;\ncout &lt;&lt; z &lt;&lt; endl;\n</code></pre>\n\n<p>也可以：<br />\n①<code>string</code>和<code>char</code>。<br />\n②<code>string</code>和<code>char</code>数组。<br />\n③<code>string</code>和 字符串常量。</p>\n\n<p>但注意，必须要有一个<code>string</code>参与进来。</p>\n\n\n<pre><code class=\"language-cpp\">cout &lt;&lt; &quot;ab&quot; + &quot;cd&quot; &lt;&lt; endl; // 都是字符串常量，没有 string 类型，会报错。\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<h1>下标1开始</h1>\n\n<p>习惯下标从$1$开始的技巧。</p>\n\n\n<pre><code class=\"language-cpp\">string s;\ncin &gt;&gt; s;\n\nint n = s.size();\ns = '#' + s; // 随便放一个字符到开始位置。这时候s.size()也同时加1了\n</code></pre>\n\n<h1>子串</h1>\n\n<h2>概念</h2>\n<p>子串，从原本的字符串中，取连续的一段。</p>\n\n<p>例如，对于字符串 \"hello\"：</p>\n<blockquote>\n<p>\"he\" 是\"hello\"的子串（取前2个字符）<br />\n\"ell\" 是\"hello\"的子串（取第2-4个字符）<br />\n\"lo\" 是\"hello\"的子串（取最后2个字符）<br />\n\"hlo\" 不是\"hello\"的子串（因为字符不连续）</p>\n</blockquote>\n\n<h2>取子串函数</h2>\n\n<p><code>substr</code>函数。</p>\n\n<p>用法1：<br />\n①第一个参数，代表起始下标。<br />\n②第二个参数，代表取的子串的长度。如果超过末尾，那么只取到末尾。</p>\n<pre><code class=\"language-cpp\">\nstring s = &quot;bigApple&quot;;\nstring t = s.substr(2, 3);\ncout &lt;&lt; t &lt;&lt; endl; // gAp\n\nt = s.substr(2, 10); // gApple\ncout &lt;&lt; t &lt;&lt; endl;\n\n</code></pre>\n\n<p>用法2：<br />\n只写一个参数，这个参数代表起始下标。<br />\n从起始下标开始一直取到末尾。</p>\n<pre><code class=\"language-cpp\">\nstring s = &quot;apple&quot;;\nstring t = s.substr(3); // le\ncout &lt;&lt; t &lt;&lt; endl;\n\n</code></pre>\n\n<h1>插入</h1>\n<h2>末尾</h2>\n\n<p><code>+=</code>运算符，往 <code>string</code> 末尾塞一个元素。<br />\n<code>push_back</code> 函数，往 <code>string</code> 末尾塞一个元素。</p>\n\n\n<pre><code class=\"language-cpp\">string s = &quot;hi &quot;;\ns += 'w';\ncout &lt;&lt; s;\n</code></pre>\n\n<h2>任意位置</h2>\n\n<p><code>insert</code>函数，可以往指定下标的前面插入内容。</p>\n\n<p>用法：</p>\n<pre><code class=\"language-cpp\">int main() {\n    string s = &quot;apple&quot;;\n    s.insert(3, &quot;red&quot;); // 下标3的前面插入red\n    cout &lt;&lt; s;\n\n    return 0;\n}\n</code></pre>\n<p>``</p>\n\n<p>可以使用拼接和<code>substr</code>代替，例如：</p>\n\n\n<pre><code class=\"language-cpp\">\nint main() {\n    string s = &quot;eat apple&quot;;\n    s = s.substr(0, 3) + &quot; red&quot; + s.substr(3);\n    // s.substr(0, 3)就是s[0]s[1]s[2]总共3个字符\n    // 因此s.substr(0, 3)和s.substr(3)就在s[2]和s[3]中间分开\n    cout &lt;&lt; s &lt;&lt; endl;\n\n    return 0;\n}\n\n</code></pre>\n\n<h1>删除</h1>\n\n<h2>末尾</h2>\n\n<p><code>pop_back</code>函数，删除 <code>string</code> 末尾一个字符。</p>\n\n\n<pre><code class=\"language-cpp\">string s = &quot;abc&quot;;\ns.pop_back(); // 变成 &quot;ab&quot;;\ncout &lt;&lt; s;\n</code></pre>\n\n<h2>任意位置</h2>\n\n<p><code>erase</code>函数，从指定下标开始删除指定数量个字符。</p>\n<pre><code class=\"language-cpp\">int main() {\n    string s = &quot;apple&quot;;\n    s.erase(1, 2);  // 删除下标1开始删除，共2个\n    cout &lt;&lt; s;\n\n    return 0;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<p>可以使用拼接和<code>substr</code>代替，例如：</p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    string s = &quot;eat red apple&quot;;\n    s = s.substr(0, 3) + s.substr(7);\n    // s.substr(0, 3)就是s[0]s[1]s[2]总共3个字符\n    // s.substr(7)就是s[7]到末尾\n    // 它们拼接起来就少了s[3]s[4]s[5]s[6]这几个字符，相当于删除了\n    cout &lt;&lt; s &lt;&lt; endl;\n\n    return 0;\n}\n</code></pre>\n\n<h2>清除</h2>\n\n<p><code>clear</code>函数，删除全部字符。<br />\n和将空串赋值给<code>string</code>是一个效果。</p>\n\n\n<pre><code class=\"language-cpp\">string s = &quot;abc&quot;;\ns.clear();\ncout &lt;&lt; s.size() &lt;&lt; endl;\ncout &lt;&lt; s &lt;&lt; endl;\n\nstring s = &quot;abc&quot;;\ns = &quot;&quot;;\ncout &lt;&lt; s.size() &lt;&lt; endl;\ncout &lt;&lt; s &lt;&lt; endl;\n</code></pre>\n\n<h1>查询</h1>\n\n<h2>随机访问</h2>\n\n<p>可以随机访问，也就是像普通数组一样。</p>\n\n\n<pre><code class=\"language-cpp\">string s;\ncin &gt;&gt; s;\ncout &lt;&lt; s[0] &lt;&lt; endl;\n</code></pre>\n\n<h2>获得子串</h2>\n<p><code>substr</code>函数，可以获得一段子串。</p>\n\n\n<pre><code class=\"language-cpp\">string s = &quot;hello world&quot;;\n\ncout &lt;&lt; s.substr(2, 4) &lt;&lt; endl;  // 第1个参数是下标，第2个参数是长度\nstring t = s.substr(2, 4);       // 得到的是string类型\ncout &lt;&lt; t &lt;&lt; endl;\n\ncout &lt;&lt; s.substr(2) &lt;&lt; endl;  // 如果没有第2个参数，就到末尾\n</code></pre>\n<h2>空</h2>\n<p><code>empty</code>函数，查询 <code>string</code> 是不是空的。<br />\n如果是空串得到<code>true</code>，否则得到<code>false</code>。</p>\n\n\n<pre><code class=\"language-cpp\">string s;\ncout &lt;&lt; s.empty() &lt;&lt; endl;\ncin &gt;&gt; s;\ncout &lt;&lt; s.empty() &lt;&lt; endl;\n</code></pre>\n\n<h2>迭代器</h2>\n\n<p>迭代器，可以当作指针来理解。<br />\n<code>begin</code>函数，可以得到第一个字符的迭代器。<br />\n<code>end</code>函数，可以得到末尾字符的下一个迭代器，这个迭代器代表的元素不存在。</p>\n\n<p>迭代器自增，得到前一个元素的迭代器。<br />\n迭代器自减，得到后一个元素的迭代器。</p>\n\n<p>迭代器加一个数字 $x$，得到往后第 $x$ 元素的迭代器。</p>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<h1>比较</h1>\n\n<p>可以比较大小，是否相同。<br />\n按照字典序比较。</p>\n\n\n<pre><code class=\"language-cpp\">string s = &quot;abc&quot;, t = &quot;cde&quot;;\ncout &lt;&lt; (s &lt; t) &lt;&lt; endl;\ncout &lt;&lt; (s &gt; t) &lt;&lt; endl;\n\ncout &lt;&lt; (s &lt;= t) &lt;&lt; endl;\ncout &lt;&lt; (s &gt;= t) &lt;&lt; endl;\n\ncout &lt;&lt; (s == t) &lt;&lt; endl;  // 相同就是等于\ncout &lt;&lt; (s != t) &lt;&lt; endl;  // 不相同就是不等于\n\n</code></pre>\n\n<h1>读入一行</h1>\n\n<p><code>getline</code>函数，可以读入一行字符串。<br />\n遇到换行停止。</p>\n\n<p><code>cin</code>忽略空格等空白字符的。如果需要管空格（比如数空格的数量），那就得用<code>getline</code>函数。</p>\n\n<p>如果不需要管空格之类的不可见字符，就不要使用<code>getline</code>函数。</p>\n\n\n<pre><code class=\"language-cpp\">string s;\ngetline(cin, s);\ncout &lt;&lt; s.size() &lt;&lt; endl;\ncout &lt;&lt; s &lt;&lt; endl;\n</code></pre>\n\n<h1>sort</h1>\n\n<p>按照<code>ASCII</code>码排序。</p>\n\n\n<pre><code class=\"language-cpp\">sort(s.begin(), s.end());\n</code></pre>\n\n<h1>时间复杂度</h1>\n\n<p>to be continue</p>";

/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["280-杂项/20-标准库/30-STL容器/20-vector"]="<p><code>STL</code>容器，动态数组，可以改变长度的数组。</p>\n\n<p>需要不确定长度或者需要改变长度，可以选择使用<code>vector</code>。</p>\n\n<h1>常用函数</h1>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<table>\n<thead>\n<tr>\n<th>函数</th>\n<th>功能</th>\n<th>时间复杂度</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>vector&lt;int&gt;a</code></td>\n<td>定义一个空的数组<code>a</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>vector&lt;int&gt;a(10)</code></td>\n<td>定义一个有$10$个元素的数组<code>a</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>vector&lt;int&gt;a(10,5)</code></td>\n<td>定义一个有$10$个$5$的数组<code>a</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>[]</code></td>\n<td>随机访问</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>push_back</code></td>\n<td>数组末尾插入元素</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>pop_back</code></td>\n<td>数组末尾删除元素</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>back</code></td>\n<td>获得末尾元素的引用</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>size</code></td>\n<td>元素个数</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>empty</code></td>\n<td>是否为空</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>begin</code></td>\n<td>首元素的迭代器</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>end</code></td>\n<td>末尾元素的下一个的迭代器</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>insert</code></td>\n<td>数组任意位置插入元素</td>\n<td>$O(n)$</td>\n</tr>\n<tr>\n<td><code>erase</code></td>\n<td>数组任意位置删除元素</td>\n<td>$O(n)$</td>\n</tr>\n<tr>\n<td><code>for auto</code></td>\n<td>循环整一个数组</td>\n<td>$O(n)$</td>\n</tr>\n<tr>\n<td><code>clear</code></td>\n<td>删除全部元素</td>\n<td>$O(n)$</td>\n</tr>\n<tr>\n<td><code>sort</code></td>\n<td>非<code>vector</code>自带函数。排序</td>\n<td>$O(n\\log n)$</td>\n</tr>\n<tr>\n<td><code>lower_bound</code></td>\n<td>非<code>vector</code>自带函数。在升序数组中，找到第一个≥的元素</td>\n<td>$O(\\log n)$</td>\n</tr>\n<tr>\n<td><code>upper_bound</code></td>\n<td>非<code>vector</code>自带函数。在升序数组中，找到第一个&gt;的元素</td>\n<td>$O(\\log n)$</td>\n</tr>\n<tr>\n<td><code>swap</code></td>\n<td>非<code>vector</code>自带函数。交换</td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>front</code></td>\n<td>第一个元素，等价于<code>a[0]</code></td>\n<td>$O(1)$</td>\n</tr>\n<tr>\n<td><code>back</code></td>\n<td>最后一个元素，等价于<code>a[a.size()-1]</code></td>\n<td>$O(1)$</td>\n</tr>\n</tbody>\n</table>\n\n<h1>定义</h1>\n\n<p>定义方式主要是以下三种：</p>\n\n\n<pre><code class=\"language-cpp\">// 方式1\nvector&lt;int&gt; a; //定义一个名字叫做a的vector，里面什么都没有\nvector&lt;string&gt; b;\n// 填int即只能存int类型，填string类型即只能存string类型\n\n// 方式2\nvector&lt;int&gt; a10(10); //定义一个名字叫做a10的vector，里面10个0\nvector&lt;int&gt; a20(20); //定义一个名字叫做a20的vector，里面20个0\n\n// 方式3\nvector&lt;int&gt; a10_23(10, 23);  //定义一个名字叫做a10_23的vector，里面10个23\nvector&lt;int&gt; a10_99(10, 99);  //定义一个名字叫做a10_99的vector，里面10个99\n\n</code></pre>\n\n<h1>随机访问</h1>\n\n<p>定义之后，<code>vector</code>也能使用普通数组的基本功能，随机访问（下标）。</p>\n\n\n<pre><code class=\"language-cpp\">\nint n;\ncin &gt;&gt; n;\nvector&lt;int&gt; a(n); // n个元素，下标0到n-1\n\nfor (int i = 0; i &lt; n; ++i) {\n    cin &gt;&gt; a[i];\n}\n\nfor (int i = 0; i &lt; n; ++i) {\n    cout &lt;&lt; a[i] &lt;&lt; endl;\n}\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<blockquote>\n<p>[!attention] 越界<br />\n<strong>一定要注意不要越界。</strong></p>\n<p>如：<code>vector&lt;int&gt; a(10);</code><br />\n元素有 $10$ 个，下标是 $0$ 到 $9$，因此只能使用 $a[0]$ 到 $a[9]$。<br />\n$a[-1],a[-2],a[10],a[11],a[100000] \\ldots$ 都是错误的。<br />\n可能出现 <code>答案错误</code>，<code>运行错误</code>。<br />\n</p>\n</blockquote>\n\n<h1>插入末尾</h1>\n\n<p><code>push_back</code> 函数，往 <code>vector</code> 末尾添加一个元素。</p>\n<pre><code class=\"language-cpp\">\nvector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\na[1] = 3;        // 当前：[1, 3, 3]\na[2] = 1;        // 当前：[1, 3, 1]\nfor (int i = 0; i &lt; 3; i++) {\n    cout &lt;&lt; a[i] &lt;&lt; endl;\n}\n</code></pre>\n\n<h1>删除末尾</h1>\n\n<p><code>pop_back</code>函数，删除<code>vector</code>末尾一个元素。</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\na.pop_back();// 当前：[1, 2]\nfor (int i = 0; i &lt; 2; i++) {\n    cout &lt;&lt; a[i] &lt;&lt; endl;\n}\n</code></pre>\n\n<h1>末尾元素</h1>\n\n<p>可以使用<code>back</code>获得末尾元素。</p>\n\n<p><code>back</code>得到的是末尾元素的引用，可以用来修改末尾元素。</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\ncout &lt;&lt; a.back() &lt;&lt; endl;\n\n// a.back() 是一个引用，可以通过 a.back() 修改最后一个\na.back() = 2333;   // 当前：[1, 2, 2333]\ncout &lt;&lt; a.back() &lt;&lt; endl;\n</code></pre>\n\n<h1>元素个数</h1>\n\n<p><code>size</code>函数，查询<code>vector</code>的元素个数。</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\n\ncout &lt;&lt; a.size() &lt;&lt; endl;\n\n// 可用于循环整一个vector\nfor (int i = 0; i &lt; a.size(); ++i) {\n    cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n}\n</code></pre>\n<p>注意：<br />\n<code>size</code>的返回值是<strong>无符号</strong>的，它的类型是只能存储非负整数，做减法的时候需要特别注意。<br />\n可以通过强制类型转换避免出问题：<code>(int)a.size()</code>。</p>\n<h1>空</h1>\n<p><code>emtpy</code>函数，查询<code>vector</code>是不是空的。即：<br />\n如果元素个数是<code>0</code>，<code>emtpy</code>函数得到<code>true</code>；<br />\n如果元素个数不是<code>0</code>，<code>emtpy</code>函数得到<code>false</code>。</p>\n\n<p>（可以使用<code>size</code>函数代替。）</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;  // 初始：[]\ncout &lt;&lt; a.empty() &lt;&lt; endl; // a.emtpy() 等同于 !a.size()\n\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\n\ncout &lt;&lt; a.empty() &lt;&lt; endl;\n</code></pre>\n\n<h1>迭代器</h1>\n\n<p><code>begin</code>函数可以获得首元素的迭代器；<br />\n<code>end</code>函数可以获得末尾元素的下一个位置的迭代器，这个迭代器代表的元素不存在。</p>\n\n<p>①迭代器类型：<br />\n迭代器的类型名字相对很长。例如，<code>vector&lt;int&gt;</code>的迭代器类型是<code>vector&lt;int&gt;::iterator</code>。</p>\n\n<p>②<code>auto</code>关键字代替：<br />\n可以使用<code>C++11</code>的<code>auto</code>关键字代替。<code>auto</code>关键字能够推断出任意类型。</p>\n\n<p>③迭代器取值：<br />\n迭代器可以当作指针来理解，在迭代器前面加上星号<code>*</code>可以得到存储的值。</p>\n\n<p>④迭代器移动：<br />\n可以通过<code>++</code>运算符变成下一个元素的迭代器，<code>--</code>运算符变成上一个元素的迭代器。<br />\n但注意不能超过<code>begin</code>和<code>end</code>。</p>\n\n<p>⑤迭代器相减：<br />\n迭代器减去首迭代器得到下标。</p>\n\n\n<pre><code class=\"language-cpp\">#include &lt;bits/stdc++.h&gt;\nusing namespace std;\nint main() {\n    vector&lt;int&gt; a;\n    a.push_back(2);\n    a.push_back(3);\n    a.push_back(5);\n    a.push_back(1);\n    a.push_back(6);\n\n    cout &lt;&lt; *a.begin() &lt;&lt; endl;\n    // a.begin() 即a[0]的迭代器, *a.begin()取出值\n    auto p = a.begin();\n        // p即a[0]的迭代器, *p取出值\n    cout &lt;&lt; *p &lt;&lt; endl; \n\n    ++p; // 往后移动，变成a[1]的迭代器\n    ++p; // 往后移动，变成a[2]的迭代器\n    ++p; // 往后移动，变成a[3]的迭代器\n\n    cout &lt;&lt; *p &lt;&lt; endl; // 相当于输出a[3]\n\n    // 减去首迭代器，得到a[3]的下标，即得到3\n    cout &lt;&lt; (p - a.begin()) &lt;&lt; endl;\n\n    // 通过迭代器循环a数组的所有元素\n    for (auto it = a.begin(); it != a.end(); ++it) {\n        cout &lt;&lt; *it &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n\n<h1>插入任意位置</h1>\n\n<p><code>insert</code> 函数，往<code>vector</code>指定位置的前面插入一个元素。</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;                // 初始：[]\na.push_back(1);               // 当前：[1]\na.push_back(2);               // 当前：[1, 2]\na.push_back(3);               // 当前：[1, 2, 3]\na.insert(a.begin(), 30);      // 当前：[30, 1, 2, 3]\na.insert(a.begin() + 2, 20);  // 当前：[30, 1, 20, 2, 3]\na.insert(a.end(), 10);        // 当前：[30, 1, 20, 2, 3, 10]\n\n// a.begin() 就是 a[0]，即a.begin()+0\n// a.begin() + 2 就是 a[2]\n\nfor (int i = 0; i &lt; 6; ++i) {\n    cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n}\n\n</code></pre>\n\n<p>需要注意，<code>insert</code>函数的时间复杂度是$O(n)$的。<br />\n因为插入的位置后面的元素都往后面整体挪动一个位置。插入的位置越靠前，挪动的元素越多。</p>\n\n<h1>删除任意位置</h1>\n\n<p><code>erase</code> 函数，删除 <code>vector</code> 指定位置的一个元素。</p>\n\n\n<pre><code class=\"language-cpp\">\nvector&lt;int&gt; a;           // 初始：[]\na.push_back(1);          // 当前：[1]\na.push_back(2);          // 当前：[1, 2]\na.push_back(3);          // 当前：[1, 2, 3]\na.erase(a.begin() + 1);  // 当前：[1, 3]\n\nfor (int i = 0; i &lt; 2; ++i) {\n    cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n}\n</code></pre>\n\n<p>需要注意，<code>erase</code>函数的时间复杂度是$O(n)$的。<br />\n因为删除的位置空出来了，因此后面的元素都往前整体挪动一个位置。删除的位置越靠前，挪动的元素越多。</p>\n<h1>for-auto</h1>\n<p><code>C++11</code>的语法。</p>\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\n\ncout &lt;&lt; a.size() &lt;&lt; endl;\n\n// 可用于循环整一个vector\n// 变量i会依次变成a数组的元素\nfor (auto i : a) { \n    cout &lt;&lt; i &lt;&lt; &quot; &quot;;\n}\n</code></pre>\n\n<p>该方法相较于下标循环法，写法更简短，且变量少一个方括号嵌套。</p>\n\n<h1>清除</h1>\n\n<p><code>clear</code>函数，删除<code>vector</code>全部元素。</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a;   // 初始：[]\na.push_back(1);  // 当前：[1]\na.push_back(2);  // 当前：[1, 2]\na.push_back(3);  // 当前：[1, 2, 3]\ncout &lt;&lt; a.size() &lt;&lt; endl;\na.clear();       // 当前：[]\n\ncout &lt;&lt; a.size() &lt;&lt; endl;\n</code></pre>\n\n<h1>sort</h1>\n\n<p><code>vector</code>使用<code>sort</code>函数的代码和普通数组不太一样，需要使用迭代器指定排序的位置。</p>\n\n<p><code>sort</code>函数第一个参数即排序的第一个位置，第二个参数即排序最后一个位置的下一个位置。</p>\n<pre><code class=\"language-cpp\">sort(a.begin(), a.end());\nsort(a.begin(), a.begin() + 4 + 1); // a[0]到a[4]排序\n</code></pre>\n\n<h1>二分函数</h1>\n\n<p>前提，在<strong>升序数组</strong>中使用。<br />\n<code>lower_bound</code>查找出第一个≥的元素。<br />\n<code>upper_bound</code>查找出第一个＞的元素。</p>\n\n<p>第一个、第二个参数指定范围，和<code>sort</code>一样。<br />\n第三个参数即要查找的数字。</p>\n\n<p>返回值是迭代器。</p>\n\n\n<pre><code class=\"language-cpp\">#include &lt;bits/stdc++.h&gt;\nusing namespace std;\nint main() {\n    vector&lt;int&gt; a;\n    a.push_back(3);\n    a.push_back(3);\n    a.push_back(7);\n    a.push_back(8);\n    a.push_back(11);\n\n    // 得到迭代器\n    auto p = lower_bound(a.begin(), a.end(), 7);\n    cout &lt;&lt; *p &lt;&lt; endl;\n    // 得到下标\n    cout &lt;&lt; lower_bound(a.begin(), a.end(), 7) - a.begin();\n\n    return 0;\n}\n</code></pre>\n\n<h1>swap</h1>\n\n<p>交换两个<code>vector</code>的时间复杂度是$O(1)$的。<br />\n因为在<code>vector</code>存的是指针，交换只需要交换它们的指针。<br />\n（如果是普通数组用 <code>swap</code> 函数，时间复杂度就是$O(n)$了）</p>\n\n\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a = {1, 2, 3};\nvector&lt;int&gt; b;\nb.push_back(3);\nb.push_back(2);\nb.push_back(3);\nb.push_back(2);\nb.push_back(1);\n\nswap(a, b);\n\nfor (auto i : a) {\n    cout &lt;&lt; i &lt;&lt; &quot; &quot;;\n}\ncout &lt;&lt; endl;\nfor (auto i : b) {\n    cout &lt;&lt; i &lt;&lt; &quot; &quot;;\n}\ncout &lt;&lt; endl;\n</code></pre>\n\n<h1>二维 vector</h1>\n\n<p>有时候数组不定长度或者某一维度长度很长，但是元素总数在$10^5$到$10^7$以内，可以使用<code>vector</code>用以下方式。</p>\n\n<h2>方式1：</h2>\n\n<p>如下，$n$行$m$列数组：</p>\n<pre><code class=\"language-cpp\">int n, m;\ncin &gt;&gt; n &gt;&gt; m;\nvector&lt;vector&lt;int&gt;&gt; a(n, vector&lt;int&gt;(m));\n</code></pre>\n<p><code>vector&lt;vector&lt;int&gt;&gt;</code>的每个元素的类型是<code>vector&lt;int&gt;</code>。<br />\n$n$个<code>vector&lt;int&gt;</code>，每个<code>vector&lt;int&gt;</code>有$m$个<code>int</code>元素。</p>\n\n<h2>方式2：</h2>\n\n<p>第一维固定长度，是普通数组。</p>\n<pre><code class=\"language-cpp\">vector&lt;int&gt; a[100005];\n</code></pre>\n\n<p>$100005$个<code>vector&lt;int&gt;</code>。</p>\n\n<p>常用于图论建图。</p>\n\n<h1><code>vector&lt;bool&gt;</code></h1>\n\n<p><code>vector&lt;bool&gt;</code> 不是<code>vector</code>，它和其它<code>vector</code>的原理不一样。<br />\n<code>vector</code>的源代码有一半的代码是单独用来写<code>vector&lt;bool&gt;</code>。</p>\n\n<p>谨慎使用<code>vector&lt;bool&gt;</code>，或者不用。<br />\n可以使用其它<code>vector</code>代替，例如<code>vector&lt;int&gt;</code>。</p>";

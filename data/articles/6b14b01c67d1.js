/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["10-语法/290-字符串/20-字符数组"]="<p>字符数组，和其它类型数组有些不一样，比较特殊。</p>\n\n<p>字符数组也可以叫做字符串。</p>\n\n<h1>使用</h1>\n\n<h2>普通数组的用法</h2>\n\n<p>普通数组的使用方法，字符数组也能用。</p>\n<pre><code class=\"language-cpp\">char s[10];\ns[0] = 'a';\ns[1] = 'b';\ncout &lt;&lt; s[0] &lt;&lt; endl;\n\nchar t[10] = {'x', 'y'};\ncout &lt;&lt; t[0] &lt;&lt; endl;\n</code></pre>\n\n<p>循环读入：</p>\n<pre><code class=\"language-cpp\">int main() {\n    char s[10];\n\n    for (int i = 0; i &lt; 5; ++i) {\n        cin &gt;&gt; s[i];\n    }\n    for (int i = 0; i &lt; 5; ++i) {\n        cout &lt;&lt; s[i];\n    }\n\n    return 0;\n}\n</code></pre>\n\n<p>注意：<br />\ncin读入字符，会忽略所有空白字符（空格，换行）。如下图：<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/f5a2da_Pasted image 20240927225418.png\" /></p>\n<div class=\"blanks\" style=\"height:2em\"></div>\n<h2>字符数组的特殊用法</h2>\n\n<p>字符数组有一些不一样。</p>\n<h3>输入输出</h3>\n\n<p>可以直接<code>cin</code>这个数组名。<br />\n读入之后，最后一个字符的下一个会变成 <code>0</code>。<br />\n（这个<code>0</code>是指<code>ASCII</code>码是 <code>0</code>。和 <code>'0'</code>不一样，<code>'0'</code>的<code>ASCII</code>码值是<code>48</code>）。</p>\n\n<p>注意：只有字符数组可以这样；其它数组不可以，会报错。</p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    char s[10];\n\n    cin &gt;&gt; s;\n    cout &lt;&lt; s; \n\n    return 0;\n}\n</code></pre>\n\n<h1>存数字</h1>\n\n<p>数字也是一个一个符号，可以存到字符数组里面。</p>\n\n<h1>字符串常量</h1>\n\n<p>字符串常量，就是带双引号的文本。其实也是一种字符串数组。</p>\n\n<p>在一开始定义字符数组时，用字符串常量赋值。<br />\n但是，定义之后就不能直接赋值了。例如</p>\n\n\n<pre><code class=\"language-cpp\">char s[100] = &quot;Hello&quot;;\n\nchar t[100] = &quot;code&quot;;\nt = &quot;algorithm&quot;; // 编译错误\n</code></pre>\n<div class=\"blanks\" style=\"height:1em\"></div>";

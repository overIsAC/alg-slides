/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["杂项/标准库/STL函数/sort"]="\n<h1>前言</h1>\n\n<p>排序就是将原本没有规则的序列，按照一定的规则排好顺序。</p>\n\n<p>C++ 自带的一个 <code>STL</code> 函数 <code>std::sort</code>，方便好用。<br />\n<code>std::sort</code> 这个函数在头文件<code>algorithm</code>里面。</p>\n\n<p><code>std::sort</code>的里面使用了很多排序算法，避免最坏的情况。</p>\n\n<p>排序带来的好处：<br />\n①如果升序排序，排序后，第一个元素最小，最后一个元素最大。<br />\n②相同的数字在排序之后，会相邻挨在一起。</p>\n<h1>参数</h1>\n<p><code>sort(首地址, 末地址的下一个, 排序规则);</code></p>\n\n<p>参数的类型，要和将要排序的每一个元素的类型相同。</p>\n\n<p>如果要使用默认比较规则，就不需要第3个参数。<br />\n（<code>int</code>、<code>long long</code>、<code>double</code>、<code>char</code>, <code>std::string</code>有默认比较规则，都是从小到大。）</p>\n\n<p>采用左闭右开的设计，\"左闭\"指首地址，\"右开\"指末地址的下一个。</p>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<h1>普通数组</h1>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<p>需要注意，下标从$1$开始和下标从$0$开始的参数不一样，因此有如下两种写法。</p>\n\n<p>其中 $a$ 是数组， $n$ 是长度。</p>\n\n<p>①下标从1开始：</p>\n<pre><code class=\"language-cpp\">sort(a + 1, a + n + 1);\n</code></pre>\n\n<p>②下标从0开始：</p>\n<pre><code class=\"language-cpp\">sort(a, a + n);\n</code></pre>\n\n<h1>具体代码</h1>\n<p>①下标从1开始：</p>\n<pre><code class=\"language-cpp\">#include&lt;bits/stdc++.h&gt;\nusing namespace std;\n\nint n, a[100];\n\nint main() {\n    cin &gt;&gt; n;\n    for (int i = 1; i &lt;= n; ++i) {\n        cin &gt;&gt; a[i];\n    }\n    sort(a + 1, a + n + 1);\n    for (int i = 1; i &lt;= n; ++i) {\n        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n\n</code></pre>\n\n<p>②下标从0开始：</p>\n\n\n<pre><code class=\"language-cpp\">#include&lt;bits/stdc++.h&gt;\nusing namespace std;\n\nint n, a[100];\n\nint main() {\n    cin &gt;&gt; n;\n    for (int i = 0; i &lt; n; ++i) {\n        cin &gt;&gt; a[i];\n    }\n    sort(a, a + n);\n    for (int i = 0; i &lt; n; ++i) {\n        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n\n<h2>解释</h2>\n<p><code>sort</code>的第一个参数是首地址，第二参数的末地址的下一个。</p>\n\n<p>①下标从$1$开始：</p>\n<pre><code class=\"language-cpp\">sort(a + 1, a + n + 1);\n</code></pre>\n<p>第一个元素是 $a[1]$，因此首地址是 $a+1$。<br />\n最后一个元素是 $a[n]$，因此末地址是 $a+n$，末地址的下一个是 $a+n+1$。</p>\n\n<p>②下标从$0$开始：</p>\n<pre><code class=\"language-cpp\">sort(a, a + n);\n</code></pre>\n<p>第一个元素是 $a[0]$，因此首地址是 $a$。<br />\n最后一个元素是 $a[n-1]$，因此末地址是 $a+n-1$，末地址的下一个是 $a+n$。</p>\n\n<h1>自定义排序</h1>\n\n<h2>单一比较规则</h2>\n\n<p><code>int</code>类型默认是从小到大的。<br />\n如果想要从大到小排序呢？</p>\n\n<p>写一个 <code>cmp</code> 函数，把<code>cmp</code>放到<code>sort</code>的第三个参数：</p>\n<pre><code class=\"language-cpp\">int cmp(int x, int y) { // 定义一个cmp函数\n    return x &gt; y;\n}\n\nint main() {\n    int n, a[100];\n    cin &gt;&gt; n;\n    for (int i = 1; i &lt;= n; ++i) {\n        cin &gt;&gt; a[i];\n    }\n    sort(a + 1, a + n + 1, cmp); // cmp函数放到第3个参数\n    for (int i = 1; i &lt;= n; ++i) {\n        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n\n<p><code>cmp</code>函数<br />\n①返回值可以是<code>int</code>，也可以是<code>bool</code>，都一样。<br />\n②一定是$2$个参数。对什么类型的数组排序，这$2$个参数就是什么类型。<br />\n③选两个数字，根据<code>cmp</code>函数是否交换。<br />\n④放在前面的数字传给<code>cmp</code>的<strong>第一个参数</strong><code>x</code>，放在后面的数组传给<strong>第二个参数</strong><code>y</code>。<br />\n⑤如果<code>cmp</code>返回值是<code>0</code>或者说返回的表达式错误，就交换；返回值是<code>1</code>或者说返回的表达式正确，就不交换。</p>\n\n<p>随便挑两个数，都满足cmp函数<br />\n①return 1 或者说满足，就不交换 <br />\n②return 0 或者说不满足，就交换<br />\n注意，如果两个数相同，一定要return 0，而不是return 1。</p>\n<h2>多种比较规则</h2>\n\n<p>有时候会要求复杂排序，就需要写更复杂的<code>cmp</code>。</p>\n\n<p><strong>写<code>cmp</code>的步骤</strong>：<br />\n①找好要比较的内容。<br />\n②确定好哪一个优先比较。<strong>哪一个优先，就用先用<code>if</code>判断它</strong>。<br />\n③依次写好比较的内容。</p>\n<div class=\"blanks\" style=\"height:1em\"></div>\n<div class=\"callout callout-note\"><div class=\"callout-title\">ℹ 例题</div><div class=\"callout-body\"><p>给$n$个正整数。排序，先让偶数在前面，奇数在后面；且偶数要从小到大，奇数要从大到小。</p>\n</div></div>\n\n<p>上面例题要比较的内容：<br />\n①奇偶数<br />\n②大小</p>\n\n<p>代码示例：</p>\n<pre><code class=\"language-cpp\">int cmp(int x, int y) {\n    // 先判断同样是奇数或偶数\n    if (x % 2 != y % 2) {   \n        // 偶数%2得0\n        // 奇数%2得1\n        // 0&lt;1，因此偶数排前面\n        return x % 2 &lt; y % 2;\n    }\n\n    // 再比较大小\n    // ①偶数 从小到大\n    // ②奇数 从大到小\n    if (x % 2 == 0) {\n        return x &lt; y;\n    } else {\n        return x &gt; y;\n    }\n}\n</code></pre>\n\n<h2>注意</h2>\n<p><code>cmp</code>函数的<code>return</code>语句，一定不要连着等于号。例如：</p>\n<pre><code class=\"language-cpp\">int cmp(int x, int y) {\n    return x &gt;= y;\n}\n</code></pre>\n\n<p>比较规则需要满足严格弱序。加上等于号就不是严格弱序，会出现不可预料的情况。</p>\n\n<p>还有一种是，如果$x$和$y$相等，一定要返回$0$。如果返回$1$也同样不是严格弱序，会出现不可预料的情况。</p>\n\n<h1>时间复杂度</h1>\n\n<p><code>std::sort</code>的时间复杂度$O(n \\log n)$，$n$指元素个数。</p>\n\n<p>像冒泡排序等两重循环的时间复杂度是 $O(n^2)$，<code>std::sort</code>明显快非常多。</p>\n\n<p>举例：<br />\n<img loading=\"lazy\" decoding=\"async\" alt=\"\" src=\"assets/attachments/7d626e_Pasted image 20240926173107.png\" /><br />\n2024年四川参加高考人数 $59.54$ 万 $\\approx600000=6\\times 10^5$。<br />\n高考排名，如果用冒泡排序，需要运行 $6\\times 10^5 \\times 6\\times 10^5=3.6\\times 10^{11}$次，大约 $10000$ 年；<br />\n但是如果使用 <code>std::sort</code> 去排序，需要运行 $6\\times 10^5 \\times  \\log( 6\\times10^5) \\approx 10^7$次，不到 $1$ 秒就算完了。</p>\n\n<p>注意：这里的元素交换的时间复杂度是$O(1)$。如果交换的时间复杂度是$O(m)$，那么使用一次<code>std::sort</code>的时间复杂度是$O(n\\log n \\times m)$。</p>\n\n<h1>结构体数组</h1>\n\n<p>to be continue...</p>\n<h2>自定义排序</h2>\n\n<p>to be continue...</p>\n<h2>重载小于号</h2>\n\n<p>to be continue...</p>\n\n<h1>对STL容器排序</h1>\n<p>to be continue...</p>\n\n<h1>greater</h1>\n\n<p><code>std::greater</code> 是一个 <code>STL</code> 类，可以用于降序排序，不用写cmp。</p>\n\n\n<pre><code class=\"language-cpp\">int main() {\n    cin &gt;&gt; n;\n    for (int i = 1; i &lt;= n; ++i) {\n        cin &gt;&gt; a[i];\n    }\n    sort(a + 1, a + n + 1, greater&lt;int&gt;());\n    for (int i = 1; i &lt;= n; ++i) {\n        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;\n    }\n\n    return 0;\n}\n</code></pre>\n\n<h1>匿名函数</h1>\n\n<p>to be continue...</p>\n\n<h1>习题</h1>\n\n<table>\n<thead>\n<tr>\n<th>题号</th>\n<th>链接</th>\n<th>备注</th>\n<th>难度</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td></td>\n<td></td>\n<td><code>int</code>数组升序</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td><code>char</code>数组排序</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td><code>int</code>数组倒序</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>输出中位数（应用）</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>前$k$名（应用）</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>分段排序</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>奇偶排序；偶数在前</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>余数分段排序；取模$3$，分$3$段，然后从小到大</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>质数排序；质数在前（函数封装）</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>平方数排序；平方数在前（函数封装）</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>相邻差值（应用）</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>绝对值排序</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n<td>二进制下1的个数排序（函数封装）</td>\n<td></td>\n</tr>\n</tbody>\n</table>\n<div class=\"blanks\" style=\"height:1em\"></div>";

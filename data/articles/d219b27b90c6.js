/* AUTO-GENERATED by build/build.py — do not edit by hand. */
(window.LART=window.LART||{})["图论/存图/邻接表之vector"]="<p>第$1$维定义成普通数组，第$2$维使用变长的<code>vector</code>。</p>\n\n<h1>无边权</h1>\n<pre><code class=\"language-cpp\">const int N = 3 + 2e5;\n\nvector&lt;int&gt; edge[N]; // edge[x]存储着所有与它相邻节点的编号，代表一条边\n\nint main() {\n    int n, m;\n    cin &gt;&gt; n &gt;&gt; m;\n    while (m--) {\n        int u, v;\n        cin &gt;&gt; u &gt;&gt; v; // 边(u, v)\n        edge[u].push_back(v);\n        // 有向图只要u到v\n        // 无向图还需要v到u\n        // edge[v].push_back(u);\n    }\n    return 0;\n}\n</code></pre>\n\n<h1>有边权</h1>\n<pre><code class=\"language-cpp\">using PII = pair&lt;int, int&gt;;\n\nconst int N = 3 + 2e5;\n\nvector&lt;PII&gt; edge[N];  // edge[x]存储着所有与它相邻节点的编号，边权，代表一条边\n\nint main() {\n    int n, m;\n    cin &gt;&gt; n &gt;&gt; m;\n    while (m--) {\n        int u, v, w;\n        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;  // 边(u, v, w)\n        edge[u].push_back({v, w});\n        // edge[v].push_back({u, w});\n    }\n\n    int x = 1;\n    for (auto e : edge[x]) {\n        int y = e.first, w = e.second;\n        // 循环一遍x节点的所有邻边\n        // x-&gt;y 边权是w\n    }\n    return 0;\n}\n</code></pre>\n\n<h1>存储其它信息</h1>\n\n<p>如果需要存储其它信息，并且信息的数量大于$2$，<code>pair</code>肯定是不够用的。可以用结构体<code>struct</code>。</p>\n\n\n<pre><code class=\"language-cpp\">const int N = 3 + 2e5;\n\nstruct Edge {\n    int y, w, id;\n};\n\nvector&lt;Edge&gt; edge[N];\n\nint main() {\n    int n, m;\n    cin &gt;&gt; n &gt;&gt; m;\n    for (int i = 1; i &lt;= m; ++i) {\n        int u, v, w;\n        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;\n        edge[u].push_back({v, w, i});  // 把边的编号i也存进去\n        // edge[v].push_back({u, w, i});\n    }\n    return 0;\n}\n</code></pre>\n\n<h1>遍历</h1>\n\n<p>①<code>vector&lt;int&gt;</code></p>\n<pre><code class=\"language-cpp\">int x = 1;\nfor (auto y : edge[x]) {\n    // 循环一遍x节点的所有邻边\n    // x-&gt;y\n}\n</code></pre>\n<p>②<code>vector&lt;PII&gt;</code></p>\n<pre><code class=\"language-cpp\">int x = 1;\nfor (auto e : edge[x]) {\n    int y = e.first, w = e.second;\n    // 循环一遍x节点的所有邻边\n    // x-&gt;y 边权是w\n}\n</code></pre>\n\n<p>③<code>vector&lt;Edge&gt;</code></p>\n<pre><code class=\"language-cpp\">int x = 1;\nfor (auto e : edge[x]) {\n        int y = e.y, w = e.w, id = e.id;\n        // 循环一遍x节点的所有邻边\n        // x-&gt;y 边权是w 编号是id\n}\n</code></pre>\n\n<h1>清空</h1>\n<pre><code class=\"language-cpp\">for (int i = 1; i &lt;= n; ++i) {\n    edge[i].clear();\n}\n</code></pre>";

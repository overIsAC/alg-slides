!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/multiset"]={title:"multiset",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 multiset",body:'<p class="lead"><code>STL</code> 容器，多重集。</p><div class="grid2v">  <div class="card">    <div class="card-title">和 set 的区别</div>    <p>和 <code>set</code> 基本一样，区别是 <code>multiset</code> 可以存储相同的元素，<b>不会去重</b>。</p>  </div>  <div class="card">    <div class="card-title">默认升序</div>    <p>底层实现是红黑树，特殊的二叉排序树。</p>  </div></div><div class="arrow-line"><code>map</code> 就是把相同数字聚合成一个东西的 <code>multiset</code>。</div>'},{tag:"02 · 常用函数（上）",title:"常用函数（上）",body:'<p class="lead">用法大部分和 <code>set</code> 一样。</p><div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>insert</code></td><td>插入</td><td>$O(\\log n)$</td></tr>    <tr><td><code>erase</code></td><td>删除，元素或迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>size</code></td><td>大小</td><td>$O(1)$</td></tr>    <tr><td><code>empty</code></td><td>是否为空</td><td>$O(1)$</td></tr>    <tr><td><code>count</code></td><td>计算指定的元素个数。$c$ 是目标元素的个数。</td><td>$O(\\log n+c)$</td></tr>    <tr><td><code>for auto</code></td><td>遍历所有元素</td><td>$O(n)$</td></tr>    <tr><td><code>begin</code></td><td>第一个元素的迭代器</td><td>$O(1)$</td></tr>  </tbody></table></div>'},{tag:"03 · 常用函数（下）",title:"常用函数（下）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>end</code></td><td>最后一个元素的<b>下一个位置</b>的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>find</code></td><td>找到某个值的迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>lower_bound</code></td><td>大于等于，得到迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>upper_bound</code></td><td>大于，得到迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>++</code></td><td>移动迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>--</code></td><td>移动迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>clear</code></td><td>清除全部</td><td>$O(n)$</td></tr>  </tbody></table></div><div class="tip-card">如果使用 <code>++</code> 遍历了所有元素，时间复杂度是 $O(n)$，而不是 $O(n\\log n)$。</div>'},{tag:"04 · 判断存在",title:"判断存在：用 find，别用 count",body:'<div class="tip-card">判断某个元素 $x$ 是否存在，<b>最好不要使用 <code>count</code> 函数</b>：$x$ 存在多少个，<code>count</code> 函数里面就会额外循环多少次。这点和 <code>map</code>、<code>set</code> 有很大的不同。</div><p class="lead">可以使用 <code>find</code> 判断。</p>',code:{label:"find 判断存在",text:["if (st.find(x) != st.end()) {","    //存在x","}"].join("\n")}},{tag:"05 · 删除",title:"删除：全部删 vs 只删一个",body:'<p class="lead"><code>erase</code> 指定值，会全部删掉；如果需要只删掉一个，而不是全部，可以通过 <code>find</code> 找出迭代器再删掉。</p>',code:{label:"erase(值) / erase(find(值))",text:["multiset<int> st;","st.insert(1); st.insert(1); st.insert(3); st.insert(3); st.insert(3); st.insert(5);","",'for (auto i : st) cout << i << " ";',"cout << endl;","","st.erase(3);  // 3全部删掉",'for (auto i : st) cout << i << " ";',"cout << endl;","","st.erase(st.find(1));  // 只删一个。需要确保一定存在",'for (auto i : st) cout << i << " ";',"cout << endl;"].join("\n")}},{tag:"06 · 习题",title:"习题",body:'<div class="ex-table">  <table>    <thead><tr><th>题号</th><th>链接</th><th>备注</th><th>难度</th></tr></thead>    <tbody>      <tr><td class="mono">CF1862G</td><td><a href="https://codeforces.com/contest/1862/problem/G" target="_blank" rel="noopener">The Great Equalizer</a></td><td>维护升序序列相邻差值</td><td>2000/蓝题</td></tr>      <tr><td class="mono">abc308_g</td><td><a href="https://atcoder.jp/contests/abc308/tasks/abc308_g" target="_blank" rel="noopener">Minimum Xor Pair Query</a></td><td>动态最小异或对，维护升序序列相邻差值。</td><td>2008/青题</td></tr>      <tr><td class="mono">CF1418D</td><td><a href="https://codeforces.com/contest/1418/problem/D" target="_blank" rel="noopener">Trash Problem</a></td><td></td><td>2100/青题</td></tr>    </tbody>  </table></div>'}]}}();
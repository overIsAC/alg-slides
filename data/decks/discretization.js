!function(){"use strict";(window.DECKS=window.DECKS||{})["基础算法/离散化"]={title:"离散化",subtitle:"基础算法",pages:[{tag:"01 · 问题",title:"想要用数值作为下标",body:'<div class="prob-card">  <div class="prob-q">想要使用数值作为下标，比如 <code>cnt[a[i]]</code>。</div>  <div class="prob-sub">如果 $a_i$ 能到 $10^9$，数组开不下，但又很想拿它当下标。</div></div><div class="grid2">  <div class="card bad">    <div class="card-title">直接开到 $10^9$ ❌</div>    <p>空间开不下。但用得上的只有 $n$ 个值。</p>  </div>  <div class="card good">    <div class="card-title">离散化 ✅</div>    <p>把要用的值收进数组 $b$，用<b>它在 $b$ 里的下标</b>代替原值当下标。</p>  </div></div>'},{tag:"02 · 举例",title:"用它排序后的下标当下标",body:'<p class="lead">要想使用 $a_i$ 当作下标，就用它排序后的下标当作下标。</p><div class="map-table">  <table>    <thead><tr><th>下标</th><th>$a_i$</th><th>排序后的 $a_i$</th></tr></thead>    <tbody>      <tr><td>$1$</td><td>$-2$</td><td>$-2$</td></tr>      <tr><td>$2$</td><td>$5$</td><td>$3$</td></tr>      <tr><td>$3$</td><td>$3$</td><td>$5$</td></tr>      <tr><td>$4$</td><td>$10^6$</td><td>$5$</td></tr>      <tr><td>$5$</td><td>$10^9$</td><td>$10^6$</td></tr>      <tr><td>$6$</td><td>$5$</td><td>$10^6$</td></tr>      <tr><td>$7$</td><td>$10^6$</td><td>$10^9$</td></tr>    </tbody>  </table></div><div class="grid2">  <div class="step-card">    <div class="n-label">$i = 3$</div>    <div class="n-step">$a_i = 3$ → 下标 $2$</div>    <div class="n-desc">原本 <code>cnt[3]</code>，换成 <code>cnt[2]</code></div>  </div>  <div class="step-card">    <div class="n-label">$i = 5$</div>    <div class="n-step">$a_i = 10^9$ → 下标 $7$</div>    <div class="n-desc">原本 <code>cnt[1000 000 000]</code>，换成 <code>cnt[7]</code></div>  </div></div><div class="tip-card">  <b>重复元素</b>排序后会占着多个下标：元素 $5$ 占着 $3,4$，只用最小的 $3$。多出来的那些本来也用不上，所以也可以把排序后的数组去重。</div>'},{tag:"03 · 代码",title:"排序 + 去重 + 二分",body:'<div class="grid2">  <div class="card">    <div class="card-title">需要定义的内容</div>    <p>原数组 $a$（长度 $n$）；数组 $b$ 存排序后的 $a$，去重后的长度 $t$。</p>  </div>  <div class="card">    <div class="card-title">四步</div>    <p>① 把 $a$ 复制到 $b$　② $b$ 升序排序</p>    <p>③ $b$ 去重，得到长度 $t$　④ 二分：拿 $a$ 的值在 $b$ 里找下标</p>  </div></div>',code:{label:"排序 + 去重 + 二分（下标从 1 开始）",text:["int n, a[N];","int t, b[N];","cin >> n;","for (int i = 1; i <= n; ++i) {","    cin >> a[i];","    b[i] = a[i];                     // ① 复制：a 与 b 长度一样","}","sort(b + 1, b + n + 1);              // ② 升序排序","t = unique(b + 1, b + n + 1) - b - 1; // ③ 去重，t 是新的长度","for (int i = 1; i <= n; ++i) {","    a[i] = lower_bound(b + 1, b + t + 1, a[i]) - b;  // ④ 二分找下标","}","// 之后直接把 a[i] 当作下标，例如 cnt[a[i]]","// 也可以不修改 a[i]，每次直接 lower_bound"].join("\n")}},{tag:"04 · 演示",title:"排序 + 去重 + 二分",body:'<p class="lead">输入 $n$ 与 $a$，看 $b$ 怎么一步步变成能用的下标表；去重之后还可以输入一个数，用二分在 $b$ 里查它的下标。</p><div class="demo-wrap"><div class="demo-body" data-app="demo-disc-compress"></div></div>',app:"demo-disc-compress"},{tag:"05 · 注意",title:"注意",body:'<div class="grid2">  <div class="card warn">    <div class="card-title">两个长度容易混淆 ⚠</div>    <p>两个数组 $a,b$，还有两个长度 $n,t$。</p>    <p>可以不去重，让 $a,b$ 都是长度 $n$，少一个变量就不容易混。</p>  </div>  <div class="card">    <div class="card-title">要查的元素都得先放进 $b$</div>    <p>所有要使用的元素都放进数组 $b$，<code>lower_bound</code> 才能在 $b$ 里查到它的位置。</p>  </div></div>'},{tag:"06 · map 编号",title:"map 编号",body:'<p class="lead">每次来一个新的元素，给它一个新编号；如果是旧元素，就拿着之前分配的编号。</p><div class="grid2">  <div class="card">    <div class="card-title">不需要预处理</div>    <p>边读边分配，来一个元素就立刻拿到编号。</p>  </div>  <div class="card">    <div class="card-title">$id$ 从 $0$ 开始递增</div>    <p><code>mp[x]</code> 存 $x$ 的编号；没出现过就分配 <code>++id</code>。</p>  </div></div>',code:{label:"map 编号",text:["map<int, int> mp;","int id;","int get(int x) {","    if (!mp.count(x)) {","        mp[x] = ++id;","    }","    return mp[x];","}","int u = get(3);","int v = get(1000000000);"].join("\n")}},{tag:"07 · 演示",title:"map 编号演示",body:'<p class="lead">表格代表 <code>mp</code>，第二列是它的编号。输入一个数点「查询」，按上一页的 <code>get()</code> 走一遍。</p><div class="demo-wrap"><div class="demo-body" data-app="demo-disc-map"></div></div>',app:"demo-disc-map"},{tag:"08 · 对比",title:"两种做法对比",body:'<div class="map-table">  <table>    <thead><tr><th></th><th>排序+去重+二分</th><th>map 编号</th></tr></thead>    <tbody>      <tr><td>离线/在线</td><td>离线</td><td>在线</td></tr>      <tr><td>预处理</td><td>$O(n\\log n)$ 的预处理</td><td>无</td></tr>      <tr><td>效率</td><td>常数小</td><td>常数大</td></tr>      <tr><td>代码</td><td>长</td><td>短</td></tr>      <tr><td>相对大小不变</td><td>是</td><td>否</td></tr>    </tbody>  </table></div><div class="grid2">  <div class="tip-card"><b>在线</b>：每来一个元素就能立刻拿到它的编号；<b>离线</b>：必须提前预处理，才能确定编号。</div>  <div class="tip-card"><b>相对大小不变</b>：离散化后值的大小关系可以当作原本值的大小关系。</div></div>'},{tag:"09 · 本质",title:"离散化的本质",body:'<div class="grid2">  <div class="card good">    <div class="card-title">给每个元素一个合理的编号</div>    <p>把很大的数据范围缩小到能够处理的下标大小，常数更小。</p>  </div>  <div class="card warn">    <div class="card-title">抹掉了元素之间的距离 ⚠</div>    <p>编号只保留大小关系，不再反映原本相差多少。</p>  </div></div><div class="arrow-line">$a_i$ 换成编号后能当下标用，但两个编号之差不再等于原值之差。</div>'},{tag:"10 · 习题",title:"习题",body:'<div class="ex-table">  <table>    <thead><tr><th>题号</th><th>题目</th><th>备注</th><th>难度</th></tr></thead>    <tbody>      <tr><td class="mono">B3694</td><td><a href="https://www.luogu.com.cn/problem/B3694" target="_blank" rel="noopener">数列离散化</a></td><td>模板</td><td><span class="diff diff-orange">橙题</span></td></tr>      <tr><td class="mono">P1496</td><td><a href="https://www.luogu.com.cn/problem/P1496" target="_blank" rel="noopener">火烧赤壁</a></td><td>离散化+差分+左闭右开</td><td><span class="diff diff-orange">橙题</span></td></tr>    </tbody>  </table></div>'}]}}();
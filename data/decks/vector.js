!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/vector"]={title:"vector",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 vector",body:'<p class="lead"><code>vector</code> 是 STL 容器，动态数组，可以改变长度的数组。</p><div class="grid2v">  <div class="card">    <div class="card-title">它是什么</div>    <p>一个动态数组，长度可以改变，不再像普通数组那样一开始就得写死。</p>  </div>  <div class="card">    <div class="card-title">什么时候用它</div>    <p>长度不确定，或者需要改变长度的时候，可以选择使用 <code>vector</code>。</p>  </div></div>'},{tag:"02 · 常用函数（一）",title:"常用函数（定义与基本信息）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>vector&lt;int&gt;a</code></td><td>定义一个空的数组<code>a</code></td><td></td></tr>    <tr><td><code>vector&lt;int&gt;a(10)</code></td><td>定义一个有$10$个元素的数组<code>a</code></td><td></td></tr>    <tr><td><code>vector&lt;int&gt;a(10,5)</code></td><td>定义一个有$10$个$5$的数组<code>a</code></td><td></td></tr>    <tr><td><code>[]</code></td><td>随机访问</td><td>$O(1)$</td></tr>    <tr><td><code>push_back</code></td><td>数组末尾插入元素</td><td>$O(1)$</td></tr>    <tr><td><code>pop_back</code></td><td>数组末尾删除元素</td><td>$O(1)$</td></tr>    <tr><td><code>size</code></td><td>元素个数</td><td>$O(1)$</td></tr>    <tr><td><code>empty</code></td><td>是否为空</td><td>$O(1)$</td></tr>    <tr><td><code>begin</code></td><td>首元素的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>end</code></td><td>末尾元素的下一个的迭代器</td><td>$O(1)$</td></tr>  </tbody></table></div>'},{tag:"03 · 常用函数（二）",title:"常用函数（增删、排序与二分）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>insert</code></td><td>数组任意位置插入元素</td><td>$O(n)$</td></tr>    <tr><td><code>erase</code></td><td>数组任意位置删除元素</td><td>$O(n)$</td></tr>    <tr><td><code>for auto</code></td><td>循环整一个数组</td><td>$O(n)$</td></tr>    <tr><td><code>clear</code></td><td>删除全部元素</td><td>$O(n)$</td></tr>    <tr><td><code>sort</code></td><td>非<code>vector</code>自带函数。排序</td><td>$O(n\\log n)$</td></tr>    <tr><td><code>lower_bound</code></td><td>非<code>vector</code>自带函数。在升序数组中，找到第一个&ge;的元素</td><td>$O(\\log n)$</td></tr>    <tr><td><code>upper_bound</code></td><td>非<code>vector</code>自带函数。在升序数组中，找到第一个&gt;的元素</td><td>$O(\\log n)$</td></tr>    <tr><td><code>swap</code></td><td>非<code>vector</code>自带函数。交换</td><td>$O(1)$</td></tr>    <tr><td><code>front</code></td><td>第一个元素的引用，等价于<code>a[0]</code></td><td>$O(1)$</td></tr>    <tr><td><code>back</code></td><td>最后一个元素的引用，等价于<code>a[a.size()-1]</code></td><td>$O(1)$</td></tr>  </tbody></table></div>'},{tag:"04 · 定义",title:"三种定义方式",body:'<p class="lead">定义方式主要是以下三种：</p>',code:{label:"vector 的三种定义",text:["// 方式1","vector<int> a; //定义一个名字叫做a的vector，里面什么都没有","vector<string> b;","// 填int即只能存int类型，填string类型即只能存string类型","","// 方式2","vector<int> a10(10); //定义一个名字叫做a10的vector，里面10个0","vector<int> a20(20); //定义一个名字叫做a20的vector，里面20个0","","// 方式3","vector<int> a10_23(10, 23);  //定义一个名字叫做a10_23的vector，里面10个23","vector<int> a10_99(10, 99);  //定义一个名字叫做a10_99的vector，里面10个99"].join("\n")}},{tag:"05 · 随机访问",title:"随机访问（下标）",body:'<p class="lead">定义之后，<code>vector</code>也能使用普通数组的基本功能，随机访问（下标）。</p>',code:{label:"随机访问",text:["int n;","cin >> n;","vector<int> a(n); // n个元素，下标0到n-1","","for (int i = 0; i < n; ++i) {","    cin >> a[i];","}","","for (int i = 0; i < n; ++i) {","    cout << a[i] << endl;","}"].join("\n")}},{tag:"06 · 越界",title:"一定要注意不要越界",body:'<div class="tip-card"><b>越界：一定要注意不要越界。</b><p>如：<code>vector&lt;int&gt; a(10);</code></p><p>元素有 $10$ 个，下标是 $0$ 到 $9$，因此只能使用 $a[0]$ 到 $a[9]$。</p><p>$a[-1],a[-2],a[10],a[11],a[100000] \\ldots$ 都是错误的。</p><p>可能出现 <code>答案错误</code>，<code>运行错误</code>。</p></div>'},{tag:"07 · 插入末尾",title:"插入末尾",body:'<p class="lead"><code>push_back</code> 函数，往 <code>vector</code> 末尾添加一个元素。</p>',code:{label:"push_back",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","a[1] = 3;        // 当前：[1, 3, 3]","a[2] = 1;        // 当前：[1, 3, 1]","for (int i = 0; i < 3; i++) {","    cout << a[i] << endl;","}"].join("\n")}},{tag:"08 · 删除末尾",title:"删除末尾",body:'<p class="lead"><code>pop_back</code>函数，删除<code>vector</code>末尾一个元素。</p>',code:{label:"pop_back",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","a.pop_back();// 当前：[1, 2]","for (int i = 0; i < 2; i++) {","    cout << a[i] << endl;","}"].join("\n")}},{tag:"09 · 末尾元素",title:"末尾元素 back",body:'<p class="lead">可以使用<code>back</code>获得末尾元素；它得到的是末尾元素的<b>引用</b>，可以用来修改末尾元素。</p>',code:{label:"back",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","cout << a.back() << endl;","","// a.back() 是一个引用，可以通过 a.back() 修改最后一个","a.back() = 2333;   // 当前：[1, 2, 2333]","cout << a.back() << endl;"].join("\n")}},{tag:"10 · 元素个数",title:"元素个数 size",body:'<p class="lead"><code>size</code>函数，查询<code>vector</code>的元素个数。</p>',code:{label:"size",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","cout << a.size() << endl;","// 可用于循环整一个vector","for (int i = 0; i < a.size(); ++i) {",'    cout << a[i] << " ";',"}"].join("\n")},footnote:'<div class="tip-card"><b>注意：</b><code>size</code>的返回值是<b>无符号</b>的，它的类型只能存储非负整数，做减法的时候需要特别注意。可以通过强制类型转换避免出问题：<code>(int)a.size()</code>。</div>'},{tag:"11 · 判空",title:"是否为空 empty",body:'<p class="lead"><code>empty</code>函数，查询<code>vector</code>是不是空的：元素个数是 <code>0</code> 得到 <code>true</code>，不是 <code>0</code> 得到 <code>false</code>。</p><div class="faint">（可以使用<code>size</code>函数代替。）</div>',code:{label:"empty",text:["vector<int> a;  // 初始：[]","cout << a.empty() << endl; // a.empty() 等同于 !a.size()","","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","cout << a.empty() << endl;"].join("\n")}},{tag:"12 · 迭代器（一）",title:"迭代器：类型、取值",body:'<p class="lead"><code>begin</code>函数可以获得首元素的迭代器；<code>end</code>函数可以获得末尾元素的下一个位置的迭代器，这个迭代器代表的元素不存在。</p><div class="grid3">  <div class="mini-step"><b>① 类型很长</b><p>例如 <code>vector&lt;int&gt;</code> 的迭代器类型是 <code>vector&lt;int&gt;::iterator</code></p></div>  <div class="mini-step"><b>② 用 auto 代替</b><p>可以用 <code>C++11</code> 的 <code>auto</code> 关键字代替，它能推断出任意类型</p></div>  <div class="mini-step"><b>③ 取值</b><p>迭代器可以当作指针来理解，在它前面加上星号 <code>*</code> 可以得到存储的值</p></div></div>'},{tag:"13 · 迭代器（二）",title:"迭代器：取出值",body:'<p class="lead">用 <code>*</code> 取出迭代器指向的值。</p>',code:{label:"取值",text:["vector<int> a;","a.push_back(2);","a.push_back(3);","a.push_back(5);","a.push_back(1);","a.push_back(6);","","cout << *a.begin() << endl;","// a.begin() 即a[0]的迭代器, *a.begin()取出值","auto p = a.begin();","    // p即a[0]的迭代器, *p取出值","cout << *p << endl;"].join("\n")}},{tag:"14 · 迭代器（三）",title:"迭代器：移动、相减、遍历",body:'<div class="card"><div class="card-title">④ 移动</div>  <p>可以通过<code>++</code>运算符变成下一个元素的迭代器，<code>--</code>运算符变成上一个元素的迭代器。但注意不能超过<code>begin</code>和<code>end</code>。</p></div>',code:{label:"移动、相减与遍历",text:["auto p = a.begin();","++p; // 往后移动，变成a[1]的迭代器","++p; // 往后移动，变成a[2]的迭代器","++p; // 往后移动，变成a[3]的迭代器","cout << *p << endl; // 相当于输出a[3]","// ⑤ 迭代器相减：减去首迭代器，得到a[3]的下标，即得到3","cout << (p - a.begin()) << endl;","// 通过迭代器循环a数组的所有元素","for (auto it = a.begin(); it != a.end(); ++it) {",'    cout << *it << " ";',"}"].join("\n")}},{tag:"15 · 插入任意位置",title:"插入任意位置 insert",body:'<p class="lead"><code>insert</code> 函数，往<code>vector</code>指定位置的前面插入一个元素。</p>',code:{label:"insert",text:["vector<int> a;                // 初始：[]","a.push_back(1);               // 当前：[1]","a.push_back(2);               // 当前：[1, 2]","a.push_back(3);               // 当前：[1, 2, 3]","a.insert(a.begin(), 30);      // 当前：[30, 1, 2, 3]","a.insert(a.begin() + 2, 20);  // 当前：[30, 1, 20, 2, 3]","a.insert(a.end(), 10);        // 当前：[30, 1, 20, 2, 3, 10]","// a.begin() 就是 a[0]，即a.begin()+0","// a.begin() + 2 就是 a[2]","for (int i = 0; i < 6; ++i) {",'    cout << a[i] << " ";',"}","// insert 是 O(n)：插入位置后面的元素整体后移一位，越靠前挪得越多"].join("\n")}},{tag:"16 · 删除任意位置",title:"删除任意位置 erase",body:'<p class="lead"><code>erase</code> 函数，删除 <code>vector</code> 指定位置的一个元素。</p>',code:{label:"erase",text:["vector<int> a;           // 初始：[]","a.push_back(1);          // 当前：[1]","a.push_back(2);          // 当前：[1, 2]","a.push_back(3);          // 当前：[1, 2, 3]","a.erase(a.begin() + 1);  // 当前：[1, 3]","","for (int i = 0; i < 2; ++i) {",'    cout << a[i] << " ";',"}","// erase 是 O(n)：删除位置后面的元素整体前移一位，越靠前挪得越多"].join("\n")}},{tag:"17 · for-auto",title:"for-auto：更短的遍历写法",body:'<p class="lead"><code>C++11</code>的语法：<code>for (auto i : a)</code> 里，变量 <code>i</code> 会依次变成数组的元素。</p><p class="faint">该方法相较于下标循环法，写法更简短，少一个方括号嵌套。</p>',code:{label:"for (auto i : a)",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","cout << a.size() << endl;","// 可用于循环整一个vector","for (auto i : a) {",'    cout << i << " ";',"}"].join("\n")}},{tag:"18 · 清除",title:"清除",body:'<p class="lead"><code>clear</code>函数，删除<code>vector</code>全部元素。</p>',code:{label:"clear",text:["vector<int> a;   // 初始：[]","a.push_back(1);  // 当前：[1]","a.push_back(2);  // 当前：[1, 2]","a.push_back(3);  // 当前：[1, 2, 3]","cout << a.size() << endl;","a.clear();       // 当前：[]","cout << a.size() << endl;"].join("\n")}},{tag:"19 · sort",title:"sort",body:'<p class="lead"><code>vector</code>使用<code>sort</code>函数的代码和普通数组不太一样，需要使用迭代器指定排序的位置：第一个参数即排序的第一个位置，第二个参数即排序最后一个位置的下一个位置。</p>',code:{label:"sort",text:["sort(a.begin(), a.end());","sort(a.begin(), a.begin() + 4 + 1); // a[0]到a[4]排序"].join("\n")}},{tag:"20 · 二分函数",title:"二分函数",body:'<p class="lead">前提，在<b>升序数组</b>中使用。<code>lower_bound</code>查找出第一个$\\ge$的元素；<code>upper_bound</code>查找出第一个$\\gt$的元素。第一个、第二个参数指定范围，和<code>sort</code>一样；第三个参数即要查找的数字。返回值是迭代器。</p>',code:{label:"lower_bound",text:["vector<int> a;","a.push_back(3);","a.push_back(3);","a.push_back(7);","a.push_back(8);","a.push_back(11);","// 得到迭代器","auto p = lower_bound(a.begin(), a.end(), 7);","cout << *p << endl;","// 得到下标","cout << lower_bound(a.begin(), a.end(), 7) - a.begin();"].join("\n")}},{tag:"21 · swap",title:"swap",body:'<p class="lead">交换两个<code>vector</code>的时间复杂度是$O(1)$的。因为<code>vector</code>里面存的是指针，交换只需要交换它们的指针。</p><p class="faint">（如果是普通数组用 <code>swap</code> 函数，时间复杂度就是$O(n)$了）</p>',code:{label:"swap",text:["vector<int> a = {1, 2, 3};","vector<int> b;","b.push_back(3);","b.push_back(2);","b.push_back(3);","b.push_back(2);","b.push_back(1);","swap(a, b);",'for (auto i : a) cout << i << " ";',"cout << endl;",'for (auto i : b) cout << i << " ";',"cout << endl;"].join("\n")}},{tag:"22 · 二维 vector",title:"二维 vector 的两种写法",body:'<p class="lead">有时候数组不定长度或者某一维度长度很长，但是元素总数在$10^5$到$10^7$以内，可以使用<code>vector</code>用以下方式。</p><p class="faint">方式1：<code>vector&lt;vector&lt;int&gt;&gt;</code>的每个元素的类型是<code>vector&lt;int&gt;</code>——$n$个<code>vector&lt;int&gt;</code>，每个<code>vector&lt;int&gt;</code>有$m$个<code>int</code>元素。</p><p class="faint">方式2：第一维固定长度，是普通数组——$100005$个<code>vector&lt;int&gt;</code>。常用于图论建图。</p>',code:{label:"二维 vector",text:["// 方式1：n 行 m 列数组","int n, m;","cin >> n >> m;","vector<vector<int>> a(n, vector<int>(m));","// 方式2：第一维是普通数组","vector<int> a[100005];"].join("\n")}},{tag:"23 · vector<bool>",title:"vector<bool> 要谨慎",body:'<p class="lead"><code>vector&lt;bool&gt;</code> 不是<code>vector</code>，它和其它<code>vector</code>的原理不一样，<code>vector</code>的源代码有一半的代码是单独用来写<code>vector&lt;bool&gt;</code>。</p><div class="grid2v">  <div class="card bad"><div class="card-title">谨慎使用</div><p>谨慎使用<code>vector&lt;bool&gt;</code>，或者不用。</p></div>  <div class="card good"><div class="card-title">可以用别的代替</div><p>可以使用其它<code>vector</code>代替，例如<code>vector&lt;int&gt;</code>。</p></div></div>'}]}}();
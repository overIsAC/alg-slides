!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/string"]={title:"string",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 string",body:'<p class="lead">动态长度的字符串。</p><div class="grid2v">  <div class="card">    <div class="card-title">它是什么</div>    <p>一个长度能变的字符串，每个元素都是<code>char</code>类型。</p>  </div>  <div class="card">    <div class="card-title">比字符数组多什么</div>    <p>有一些比字符数组更方便、好用的函数。</p>  </div></div>'},{tag:"02 · 常用函数（一）",title:"常用函数（定义、访问与增删）",body:"<div class=\"map-table\"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>string s</code></td><td>定义一个空串</td><td>$O(1)$</td></tr>    <tr><td><code>string s(5,'a')</code></td><td>定义一个有$5$个<code>a</code>的串</td><td></td></tr>    <tr><td><code>[]</code></td><td>随机访问，得到下标处的字符</td><td>$O(1)$</td></tr>    <tr><td><code>+</code></td><td>拼接两个字符串</td><td>$O(n+m)$</td></tr>    <tr><td><code>+=</code></td><td>末尾追加一个字符或一段内容</td><td>追加一个字符是$O(1)$</td></tr>    <tr><td><code>push_back</code></td><td>末尾追加一个字符</td><td>$O(1)$</td></tr>    <tr><td><code>pop_back</code></td><td>删除末尾一个字符</td><td>$O(1)$</td></tr>    <tr><td><code>size</code></td><td>字符个数</td><td>$O(1)$</td></tr>    <tr><td><code>empty</code></td><td>是否为空</td><td>$O(1)$</td></tr>    <tr><td><code>clear</code></td><td>删除全部字符</td><td>$O(n)$</td></tr>    <tr><td><code>substr</code></td><td>取子串</td><td>$O(n)$</td></tr>  </tbody></table></div>"},{tag:"03 · 常用函数（二）",title:"常用函数（插入、删除与转换）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>insert</code></td><td>在下标前面插入内容</td><td>$O(n)$</td></tr>    <tr><td><code>erase</code></td><td>从下标开始删除指定数量个字符</td><td>$O(n)$</td></tr>    <tr><td><code>begin</code></td><td>第一个字符的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>end</code></td><td>末尾字符的下一个的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>==</code>、<code>!=</code></td><td>判断两个串是否相同</td><td>$O(n)$</td></tr>    <tr><td><code>&lt;</code>、<code>&lt;=</code>、<code>&gt;</code>、<code>&gt;=</code></td><td>按字典序比较大小</td><td>$O(n)$</td></tr>    <tr><td><code>getline</code></td><td>读入一整行</td><td>$O(n)$</td></tr>    <tr><td><code>to_string</code></td><td>数字转字符串</td><td>$O(\\log n)$</td></tr>    <tr><td><code>stoi</code>、<code>stoll</code>、<code>stod</code></td><td>字符串转<code>int</code>、<code>long long</code>、<code>double</code></td><td>$O(n)$</td></tr>    <tr><td><code>sort</code></td><td>非<code>string</code>自带函数。按<code>ASCII</code>码排序</td><td>$O(n\\log n)$</td></tr>    <tr><td><code>swap</code></td><td>非<code>string</code>自带函数。交换</td><td>$O(1)$</td></tr>  </tbody></table></div>'},{tag:"04 · 定义",title:"定义",body:'<p class="lead">每个元素都是<code>char</code>类型。</p>',code:{label:"string 的定义",text:["string s1;          // 空串","string s2(5, 'a');  // \"aaaaa\"  5个a",'string s3 = "abc";  // 将字符串常量赋值给string类型',"",'char s[10] = "qwe";',"string s4 = s;  // 将字符数组赋值给string类型"].join("\n")}},{tag:"05 · 输入输出",title:"输入输出与随机访问",body:'<p class="lead">可以直接<code>cin</code>读入、<code>cout</code>输出，查询和随机访问都跟普通数组一样用下标，但<b>注意不要越界</b>。</p>',code:{label:"读入、输出、按下标访问",text:["string s;","cin >> s;  // 如果输入 hi","cout << s[0] << endl;  // h","cout << s[1] << endl;  // i","cout << s << endl;     // hi   全部输出","s[0] = 'a';","s[1] = 'a';","cout << s << endl;     // aa   全部输出","// 错误，越界了","cout << s[2] << endl;","cout << s[-1] << endl;","// 使用printf输出",'printf("%s", s.c_str()); // 得到一个char*指针'].join("\n")}},{tag:"06 · 大小与判空",title:"size 与 empty",body:'<p class="lead"><code>size</code>函数，查询<code>string</code>大小；<code>empty</code>函数，查询<code>string</code>是不是空的。</p><div class="grid2v">  <div class="card"><div class="card-title">是空串</div><p><code>empty</code>得到<code>true</code>。</p></div>  <div class="card"><div class="card-title">不是空串</div><p><code>empty</code>得到<code>false</code>。</p></div></div><div class="faint">（可以使用<code>size</code>函数代替。）</div>',code:{label:"size 与 empty",text:["string s;","cin >> s;","cout << s.size() << endl;","","cout << s.empty() << endl;","cin >> s;","cout << s.empty() << endl;"].join("\n")}},{tag:"07 · 拼接",title:"拼接",body:'<p class="lead">可以将 <code>string</code> 拼接起来。也可以：</p><div class="grid3">  <div class="mini-step"><b>①</b><p><code>string</code>和<code>char</code></p></div>  <div class="mini-step"><b>②</b><p><code>string</code>和<code>char</code>数组</p></div>  <div class="mini-step"><b>③</b><p><code>string</code>和字符串常量</p></div></div><div class="tip-card">但注意，<b>必须要有一个<code>string</code>参与进来</b>。</div>',code:{label:"拼接",text:['string s = "hi";','string t = "lihua";',"cout << s + t << endl;","","string z = s + t;","cout << z << endl;","",'cout << "ab" + "cd" << endl; // 都是字符串常量，没有 string 类型，会报错。'].join("\n")}},{tag:"08 · 下标1开始",title:"下标从 $1$ 开始的技巧",body:'<p class="lead">习惯下标从$1$开始的技巧。</p>',code:{label:"开头垫一个字符",text:["string s;","cin >> s;","","int n = s.size();","s = '#' + s; // 随便放一个字符到开始位置。这时候s.size()也同时加1了"].join("\n")}},{tag:"09 · 子串",title:"子串：取连续的一段",body:'<p class="lead">子串，从原本的字符串中，取连续的一段。</p><p class="faint">例如，对于字符串 "hello"：</p><div class="edge-steps">  <div class="edge-row"><span class="e-cond">"he"</span><span class="e-why">是"hello"的子串——取前2个字符</span></div>  <div class="edge-row"><span class="e-cond">"ell"</span><span class="e-why">是"hello"的子串——取第2-4个字符</span></div>  <div class="edge-row"><span class="e-cond">"lo"</span><span class="e-why">是"hello"的子串——取最后2个字符</span></div>  <div class="edge-row"><span class="e-cond">"hlo"</span><span class="e-why">不是"hello"的子串——因为字符不连续</span></div></div>'},{tag:"10 · 取子串",title:"取子串 substr",body:'<p class="lead"><code>substr</code>函数。</p><p class="faint">用法1：①第一个参数，代表起始下标。②第二个参数，代表取的子串的长度。如果超过末尾，那么只取到末尾。</p><p class="faint">用法2：只写一个参数，这个参数代表起始下标，从起始下标开始一直取到末尾。</p>',code:{label:"substr",text:["// 用法1：起始下标 + 长度",'string s = "bigApple";',"string t = s.substr(2, 3);","cout << t << endl; // gAp","t = s.substr(2, 10); // gApple","cout << t << endl;","// 用法2：只写起始下标",'string s = "apple";',"string t = s.substr(3); // le","cout << t << endl;"].join("\n")}},{tag:"11 · 插入末尾",title:"插入末尾",body:'<p class="lead"><code>+=</code>运算符和<code>push_back</code>函数，都往<code>string</code>末尾塞一个元素。</p>',code:{label:"+= / push_back",text:['string s = "hi ";',"s += 'w';","cout << s;"].join("\n")}},{tag:"12 · 插入任意位置",title:"插入任意位置 insert",body:'<p class="lead"><code>insert</code>函数，可以往指定下标的前面插入内容。</p>',code:{label:"insert",text:["int main() {",'    string s = "apple";','    s.insert(3, "red"); // 下标3的前面插入red',"    cout << s;","","    return 0;","}"].join("\n")}},{tag:"13 · 插入的替代写法",title:"插入：用拼接和 substr 代替",body:'<p class="lead">可以使用拼接和<code>substr</code>代替，例如：</p>',code:{label:"拼接代替 insert",text:["int main() {",'    string s = "eat apple";','    s = s.substr(0, 3) + " red" + s.substr(3);',"    // s.substr(0, 3)就是s[0]s[1]s[2]总共3个字符","    // 因此s.substr(0, 3)和s.substr(3)就在s[2]和s[3]中间分开","    cout << s << endl;","","    return 0;","}"].join("\n")}},{tag:"14 · 删除末尾",title:"删除末尾",body:'<p class="lead"><code>pop_back</code>函数，删除 <code>string</code> 末尾一个字符。</p>',code:{label:"pop_back",text:['string s = "abc";','s.pop_back(); // 变成 "ab";',"cout << s;"].join("\n")}},{tag:"15 · 删除任意位置",title:"删除任意位置 erase",body:'<p class="lead"><code>erase</code>函数，从指定下标开始删除指定数量个字符。</p>',code:{label:"erase",text:["int main() {",'    string s = "apple";',"    s.erase(1, 2);  // 下标1开始删除，共2个","    cout << s;","","    return 0;","}"].join("\n")}},{tag:"16 · 删除的替代写法",title:"删除：用拼接和 substr 代替",body:'<p class="lead">可以使用拼接和<code>substr</code>代替，例如：</p>',code:{label:"拼接代替 erase",text:["int main() {",'    string s = "eat red apple";',"    s = s.substr(0, 3) + s.substr(7);","    // s.substr(0, 3)就是s[0]s[1]s[2]总共3个字符","    // s.substr(7)就是s[7]到末尾","    // 它们拼接起来就少了s[3]s[4]s[5]s[6]这几个字符，相当于删除了","    cout << s << endl;","","    return 0;","}"].join("\n")}},{tag:"17 · 清除",title:"清除",body:'<p class="lead"><code>clear</code>函数，删除全部字符。和将空串赋值给<code>string</code>是一个效果。</p>',code:{label:"clear",text:['string s = "abc";',"s.clear();","cout << s.size() << endl;","cout << s << endl;","",'string s = "abc";','s = "";',"cout << s.size() << endl;","cout << s << endl;"].join("\n")}},{tag:"18 · 迭代器",title:"迭代器",body:'<p class="lead">迭代器，可以当作指针来理解。</p><div class="grid2v">  <div class="card"><div class="card-title"><code>begin</code></div><p>可以得到第一个字符的迭代器。</p></div>  <div class="card"><div class="card-title"><code>end</code></div><p>可以得到末尾字符的下一个迭代器，这个迭代器代表的元素不存在。</p></div></div><div class="formula-row">  <div class="fm"><span>自增</span><b>得到后一个元素的迭代器</b></div>  <div class="fm"><span>自减</span><b>得到前一个元素的迭代器</b></div>  <div class="fm"><span>加一个数字 $x$</span><b>得到往后第 $x$ 元素的迭代器</b></div></div>'},{tag:"19 · 比较",title:"比较",body:'<p class="lead">可以比较大小，是否相同。按照字典序比较。</p>',code:{label:"比较",text:['string s = "abc", t = "cde";',"cout << (s < t) << endl;","cout << (s > t) << endl;","","cout << (s <= t) << endl;","cout << (s >= t) << endl;","","cout << (s == t) << endl;  // 相同就是等于","cout << (s != t) << endl;  // 不相同就是不等于"].join("\n")}},{tag:"20 · 读入一行",title:"读入一行 getline",body:'<p class="lead"><code>getline</code>函数，可以读入一行字符串，遇到换行停止。</p><p class="faint"><code>cin</code>忽略空格等空白字符。如果需要管空格（比如数空格的数量），那就得用<code>getline</code>函数。</p><div class="tip-card"><b>注意</b>：如果不需要管空格之类的不可见字符，就不要使用<code>getline</code>函数。</div>',code:{label:"getline",text:["string s;","getline(cin, s);","cout << s.size() << endl;","cout << s << endl;"].join("\n")}},{tag:"21 · to_string",title:"to_string：数字转字符串",body:'<p class="lead">把数字转成字符串。需要<code>C++11</code>以上。</p>',code:{label:"to_string",text:["int n;","cin >> n;","string s = to_string(n);","cout << s.size();"].join("\n")}},{tag:"22 · stoi / stoll / stod",title:"字符串转数字",body:'<p class="lead"><code>stoi</code>/<code>stoll</code>/<code>stod</code>三个函数分别将字符串转成<code>int</code>、<code>long long</code>、<code>double</code>类型。需要<code>C++11</code>以上。</p>',code:{label:"stoi",text:['string s = "12";','s += "34";',"int n = stoi(s);","cout << n;"].join("\n")}},{tag:"23 · sort",title:"sort",body:'<p class="lead">按照<code>ASCII</code>码排序。</p>',code:{label:"sort",text:["sort(s.begin(), s.end());"].join("\n")}}]}}();
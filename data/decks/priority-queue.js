!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/priority_queue"]={title:"priority_queue",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 priority_queue",body:'<p class="lead"><code>STL</code>容器，优先队列，堆。</p><div class="grid2">  <div class="card">    <div class="card-title">能做什么</div>    <p>在 $\\log$ 时间复杂度内取最值、增加和删除元素。</p>  </div>  <div class="card good">    <div class="card-title">底层是什么</div>    <p>默认使用 <code>vector</code> 和二叉堆实现。</p>  </div></div>'},{tag:"02 · 常用函数",title:"常用函数",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>push</code></td><td>$O(\\log n)$</td></tr>    <tr><td><code>top</code></td><td>$O(1)$</td></tr>    <tr><td><code>pop</code></td><td>$O(\\log n)$</td></tr>    <tr><td><code>size</code></td><td>$O(1)$</td></tr>    <tr><td><code>empty</code></td><td>$O(1)$</td></tr>  </tbody></table></div><div class="arrow-line"><code>push</code>和<code>pop</code>两个会修改的函数，时间复杂度是 $\\log$；其它函数都是 $1$。</div>'},{tag:"03 · 定义与 push",title:"定义一个堆、往里塞数",code:{label:"定义与 push",text:["priority_queue<int> q; // 默认大根堆，也就是取出来都是最大值","q.push(2); // 存一个进去","q.push(2);","q.push(5);","q.push(1);"].join("\n")}},{tag:"04 · 取堆顶与元素个数",title:"取堆顶、查元素个数",code:{label:"top 与 size",text:["// 输出堆顶","cout << q.top() << endl; // 5","","// 输出元素个数","cout << q.size() << endl; // 4"].join("\n")}},{tag:"05 · 删除堆顶与判空",title:"删除堆顶、判断是否为空",code:{label:"pop 与 empty",text:["// 移除堆顶","q.pop();","cout << q.top() << endl; // 2","cout << q.size() << endl; // 3","","cout << q.empty() << endl; // 是不是空","// 没有clear函数"].join("\n")}},{tag:"06 · 小根堆",title:"小根堆怎么写",body:'<p class="faint">注意，里面填 <code>int</code> 的位置，如果想要填别的类型，一定要一样。</p>',code:{label:"小根堆的定义方式",text:["priority_queue<int, vector<int>, greater<int>> q;","// 得c++11，不然会被识别成右移运算符","","priority_queue<int, vector<int>, greater<int> > q;","// C++11以下"].join("\n")}},{tag:"07 · 常数对比",title:"和 set、map 比常数",body:'<p class="lead">很明显，<code>set</code>、<code>map</code>有<code>priority_queue</code>所有功能，而且更丰富。</p><div class="arrow-line">但是 <code>priority_queue</code> 的常数会更小，$10^6$ 以上元素的个数时需要注意。</div><div class="grid2v">  <div class="card">    <div class="card-title">常数指什么</div>    <p>常数就是指，同样效果的代码，花费的时间不同。</p>  </div>  <div class="card">    <div class="card-title">为什么会有差别</div>    <p>因为里面的代码原理或者具体实现不一样。</p>  </div></div>'},{tag:"08 · 自定义结构体",title:"自定义结构体",body:'<p class="lead">需要以下条件之一：</p><div class="grid2">  <div class="card"><div class="card-title">① 重载小于号</div><p>给结构体自己写 <code>operator&lt;</code>。</p></div>  <div class="card"><div class="card-title">② 类的仿函数</div><p>单独写一个比较类，放进模板参数里。</p></div></div><div class="tip-card"><b>注意</b>：<code>priority_queue</code> 自定义的比较和 <code>sort</code> 的 <code>cmp</code> 函数是<b>反过来</b>的。</div>'},{tag:"09 · 重载小于号（写法）",title:"写法一：重载小于号",body:'<p class="lead">大根堆，每次取最大的。（和 <code>sort</code> 是反过来的）</p>',code:{label:"给结构体写 operator<",text:["struct Node {","    int x;","    int operator<(const Node& other) const {","        return x < other.x; // 如果要小根堆，就改成 大于号，也就是x>other.x","    }","};"].join("\n")}},{tag:"10 · 重载小于号（用法）",title:"写法一：怎么用",code:{label:"priority_queue<Node>",text:["int main() {","    priority_queue<Node> q;","","    q.push({1});","    q.push({3});","    q.push({2});","","    while (q.size()) {","        cout << q.top().x << endl;","        q.pop();","    }","    // 输出顺序是 3 2 1","","    return 0;","}"].join("\n")}},{tag:"11 · 仿函数（写法）",title:"写法二：仿函数",code:{label:"单独写一个比较类",text:["struct Node {","    int x;","};","","struct Cmp {","    bool operator()(const Node& a, const Node& b) const {","        return a.x < b.x; // 如果要小根堆，改成：return a.x > b.x;","    }","};"].join("\n")}},{tag:"12 · 仿函数（用法）",title:"写法二：怎么用",code:{label:"priority_queue<Node, vector<Node>, Cmp>",text:["int main() {","    priority_queue<Node, vector<Node>, Cmp> q;","","    q.push({1});","    q.push({3});","    q.push({2});","","    while (!q.empty()) {","        cout << q.top().x << endl;","        q.pop();","    }","}"].join("\n")}}]}}();
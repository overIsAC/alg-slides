!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/pair"]={title:"pair",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 pair",body:'<p class="lead"><code>pair</code> 是标准库中定义的一个类模板，用于将两个变量关联在一起，组成一个「对」；两个变量的数据类型可以是不同的。</p><div class="grid2v">  <div class="card">    <div class="card-title">它是什么</div>    <p>把两个变量绑在一起，组成一个「对」，两个变量的类型可以不一样。</p>  </div>  <div class="card">    <div class="card-title">什么时候用它</div>    <p>需要把有关联的数据<b>捆绑存储、处理</b>的场景，用它可以少定义一个结构体。</p>  </div></div>'},{tag:"02 · 定义与取值",title:"定义与取值",body:"",code:{label:"first 与 second",text:["pair<int, int> a = {1, 2};","cout << a.first << endl; // 1 第一个元素","cout << a.second << endl; // 2 第二个元素","","pair<int, string> b;","b.first = 2;",'b.second = "lemon";','cout << b.first << " " << b.second;'].join("\n")}},{tag:"03 · 自带比较与排序",title:"自带比较，能直接排序",body:'<p class="faint">已经重载了小于号<code>&lt;</code>，还有其它比较符号；因此<code>pair</code>数组可以不写重载小于号或者<code>cmp</code>。</p><div class="arrow-line">排序规则：先按照第一个元素 <code>first</code> 升序；如果 <code>first</code> 相同，就按照第二个元素 <code>second</code> 升序。</div>',code:{label:"给 pair 数组排序",text:["pair<int, int> a[5];","a[1] = {3, 4};","a[2] = {1, 2};","a[3] = {3, 2};","sort(a + 1, a + 3 + 1);","for (int i = 1; i <= 3; ++i) {",'    cout << a[i].first << " " << a[i].second << endl;',"}"].join("\n")}},{tag:"04 · 和结构体比较",title:"和结构体比较",body:'<div class="grid2">  <div class="card good">    <div class="card-title">pair 优点</div>    <p>① <code>C++</code>自带。</p>    <p>② 自带比较规则，大部分情况可以不写 <code>cmp</code>。</p>    <p>③ 不用额外定义结构与重载运算符，用起来更简便。</p>  </div>  <div class="card bad">    <div class="card-title">pair 缺点</div>    <p>① <code>first</code> 和 <code>second</code> 这两个变量较长，命名不如自定义结构体清晰。</p>    <p>② 需要把两个以上的变量关联起来时，自定义结构体更合适。</p>  </div></div>'}]}}();
!function(){"use strict";(window.DECKS=window.DECKS||{})["杂项/标准库/STL容器/map"]={title:"map",subtitle:"杂项 · STL容器",pages:[{tag:"01 · 概念",title:"什么是 map",body:'<p class="lead"><code>STL</code> 容器，映射。</p><div class="grid2v">  <div class="card">    <div class="card-title">键值对</div>    <p>存储 <code>key-value</code>，一对一对的元素；在 <code>map</code> 里面以 <code>pair</code> 的形式存在。</p>  </div>  <div class="card">    <div class="card-title">key 唯一</div>    <p>每个 <code>key</code> 只能有一个，<code>value</code> 可以很多个。</p>  </div>  <div class="card">    <div class="card-title">默认升序</div>    <p>按照 <code>key</code> 升序；底层实现是红黑树，特殊的二叉排序树。</p>  </div>  <div class="card">    <div class="card-title">可以当 set 用</div>    <p>如果不管 <code>value</code>，可以把 <code>map</code> 当作 <code>set</code> 使用。</p>  </div></div><div class="arrow-line">优势：可以用任意数字，甚至任意类型的变量当作下标。</div>'},{tag:"02 · 常用函数（上）",title:"常用函数（上）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>[]</code></td><td>如同普通数组一样使用下标</td><td>$O(\\log n)$</td></tr>    <tr><td><code>erase</code></td><td>删除，元素或迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>size</code></td><td>大小</td><td>$O(1)$</td></tr>    <tr><td><code>empty</code></td><td>是否为空</td><td>$O(1)$</td></tr>    <tr><td><code>count</code></td><td>是否存在</td><td>$O(\\log n)$</td></tr>    <tr><td><code>for auto</code></td><td>遍历所有元素</td><td>$O(n)$</td></tr>  </tbody></table></div>'},{tag:"03 · 常用函数（下）",title:"常用函数（下）",body:'<div class="map-table"><table>  <thead><tr><th>函数</th><th>功能</th><th>时间复杂度</th></tr></thead>  <tbody>    <tr><td><code>begin</code></td><td>第一个元素的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>end</code></td><td>最后一个元素的<b>下一个位置</b>的迭代器</td><td>$O(1)$</td></tr>    <tr><td><code>lower_bound</code></td><td>大于等于 <code>key</code>，得到迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>upper_bound</code></td><td>大于 <code>key</code>，得到迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>++</code></td><td>移动迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>--</code></td><td>移动迭代器</td><td>$O(\\log n)$</td></tr>    <tr><td><code>clear</code></td><td>清除全部</td><td>$O(n)$</td></tr>    <tr><td><code>find</code></td><td>找到某个 <code>key</code> 的迭代器</td><td>$O(\\log n)$</td></tr>  </tbody></table></div><div class="tip-card">如果使用 <code>++</code> 遍历了所有元素，时间复杂度是 $O(n)$，而不是 $O(n\\log n)$。</div>'},{tag:"04 · 使用：下标存取",title:"下标存取",code:{label:"mp[key] = value",text:["map<int, int> mp;","","mp[1] = 2; // 就像普通数组一样","mp[2] = 4;","mp[2000000000] = 2333; // 但是下标可以很大，而普通数组不行","","cout << mp[1] << endl; // 2","cout << mp[2] << endl; // 4","cout << mp[2000000000] << endl; // 2333","","// mp里面有3个键值对","// {1, 2}, {2, 4}, {2000000000, 2333}","// 或者说mp有3个元素，分别是：1, 2, 2000000000","cout << mp.size() << endl; // 3 元素个数"].join("\n")}},{tag:"05 · 使用：删除与清空",title:"删除、清空、判断存在",code:{label:"erase / clear / size / empty / count",text:["mp.erase(2); // 删除指定元素","","mp.clear(); // 全部清除","cout << mp.size() << endl; // 0","cout << mp.empty() << endl; // true","","cout << mp.count(5) << endl; // 0 判断5是否在mp里面","mp[5] = 233;","cout << mp.count(5) << endl; // 1"].join("\n")}},{tag:"06 · 使用：遍历",title:"遍历：范围 for",code:{label:"for (auto i : mp)",text:["mp[5] = 6;","mp[4] = 7;","mp[3] = 8;","mp[2] = 9;","mp[1] = 10;","for (auto i : mp) {","    // i依次成为每一个键值对，按照key升序","    // i是pair类型","    // mp是map<int, int> 类型，i就是pair<int, int> 类型",'    cout << i.first << " " << i.second << endl;',"}","cout << endl;"].join("\n")}},{tag:"07 · 使用：迭代器",title:"迭代器与 lower_bound",body:'<p class="lead">迭代器可以认为是指针，用它的值要加星号 <code>*</code>。</p><div class="grid2v">  <div class="card"><div class="card-title">指向哪</div><p><code>mp.begin()</code> 指向第一个元素的迭代器；<code>mp.end()</code> 指向最后一个元素的<b>下一个位置</b>的迭代器。</p></div>  <div class="card"><div class="card-title">怎么取键和值</div><p>每一个键值对都是 <code>pair</code>，用 <code>-&gt;first</code> 取 <code>key</code>、<code>-&gt;second</code> 取 <code>value</code>。</p></div></div>',code:{label:"begin / lower_bound / upper_bound",text:['cout << mp.begin()->first << " " << mp.begin()->second << endl; // 1 10',"auto p = mp.lower_bound(3); // 得到大于等于元素的迭代器","if (p != mp.end()) { // 如果没有，p就是mp.end()",'    cout << p->first << " " << p->second << endl; // 3 8',"}","auto p2 = mp.upper_bound(3); // 得到大于元素的迭代器","if (p2 != mp.end()) { // 如果没有，p2就是mp.end()",'    cout << p2->first << " " << p2->second << endl; // 4 7',"}"].join("\n")}},{tag:"08 · 注意：不存在的 key",title:"不存在的 key",body:'<p class="lead">如果 <code>map</code> 里面不存在要找的 <code>key</code>，使用方括号会先创建一个元素，<code>map</code> 大小会增加一。</p><div class="grid2v">  <div class="card"><div class="card-title">value 是 int</div><p>创建一个 $0$。</p></div>  <div class="card"><div class="card-title">value 是 string</div><p>创建一个空串。</p></div></div>',code:{label:"方括号会偷偷加元素",text:["map<int, string> mp;","cout << mp.size() << endl;","",'cout << mp[5].size() << endl; // 0 没有5这个数字，自动创建，并且对应""，空串size是0',"","cout << mp.size() << endl; // 1 mp的大小变了。这是一定要注意的事情","","if (mp.count(1)) { // 应该先用count判断是否存在，再决定使用","    cout << mp[1] << endl;","}"].join("\n")}},{tag:"09 · 注意：迭代器失效",title:"迭代器失效",body:'<div class="tip-card">被删除的迭代器就不能再使用。<b>只有删除</b>会让迭代器失效。</div>',code:{label:"删掉之后不能再动它",text:["map<int, int> mp;","mp[1] = 2;","mp[5] = 3;","","auto it = mp.find(5);","","mp.erase(it);",'cout << it->first << " " << it->second << endl; // 错误，未定义行为'].join("\n")}},{tag:"10 · 注意：一边删除一边遍历",title:"一边删除一边遍历",body:'<p class="lead">前面有一个遍历是 <code>for auto</code>。但如果需要一边删除一边遍历，就不能使用这种循环——因为删除会造成迭代器失效，只能使用迭代器遍历。</p>',code:{label:"迭代器遍历 + 删除",text:["for (auto it = mp.begin(); it != mp.end();) {","","    if (xxx条件) {","        it = mp.erase(it);","        // 当前位置删掉","        // erase的返回值是当前的下一个位置","    } else {","        ++it;","    }","}"].join("\n")}},{tag:"11 · 自定义结构体",title:"自定义结构体",body:'<p class="lead">需要以下条件之一：</p><div class="grid2v">  <div class="card"><div class="card-title">① 重载小于号</div></div>  <div class="card"><div class="card-title">② 类的仿函数</div></div></div>'},{tag:"12 · 重载小于号",title:"写法一：重载小于号",code:{label:"map<Node, int>",text:["struct Node {","    int x, y;","    bool operator<(const Node& other) const {","        if (x != other.x) {","            return x < other.x;","        }","        return y < other.y;","    }","};","int main() {","    map<Node, int> mp;","    mp[{1, 2}] = 10;","    mp[{5, 3}] = 20;","    cout << mp[{1, 2}] << endl; // 10","    cout << mp[{5, 5}] << endl; // 0","    return 0;","}"].join("\n")}},{tag:"13 · 类的仿函数",title:"写法二：类的仿函数",code:{label:"map<Node, int, Cmp>",text:["struct Node {","    int x, y;","};","","struct Cmp {","    bool operator()(const Node& a, const Node& b) const {","        if (a.x != b.x) {","            return a.x < b.x;","        }","        return a.y < b.y;","    }","};","map<Node, int, Cmp> mp;  // 第二个类型，带有比较规则的结构体"].join("\n")}},{tag:"14 · 注意",title:"每个变量都要比较",code:{label:"错误示例：只比较了 x",text:["// 定义多个变量的结构体，每个变量都要比较：只比较 x 的话，y 不同的会被当作同一个东西","struct Node {","    int x, y;","    bool operator<(const Node& other) const {","        return x < other.x;","    }","};","int main() {","    map<Node, int> mp;","    mp[{1, 2}] = 10; mp[{1, 3}] = 20;","    // {1,2}和{1,3}被当作同一个东西，都是找到mp里面{1,2}这个key","    cout << mp.size() << endl;                                         // 1","    cout << mp[{1, 2}] << endl;                                        // 20","    cout << mp[{1, 3}] << endl;                                        // 20","    cout << mp.begin()->first.x << \" \" << mp.begin()->first.y << ' ';  // 1 2","    cout << mp.begin()->second;                                        // 20","    return 0;","}"].join("\n")}},{tag:"15 · 离散化",title:"离散化",body:'<div class="card"><div class="card-title">map编号</div><p><a href="#基础算法/离散化">离散化 · map编号</a></p></div>'},{tag:"16 · 容器嵌套：map 套 map",title:"map<int, map<int, int>>",body:'<p class="lead">有一些容器嵌套的代码是很好写、好用。自然是有代价的，比如运行时间增多。</p><p>可以当作二维数组使用，两个维度都很大。</p>',code:{label:"二维",text:["map<int, map<int, int>> mp;","mp[2][12] = 13;"].join("\n")}},{tag:"17 · 容器嵌套：map 套 vector",title:"map<int, vector<int>>",body:'<p class="lead">想使用 <code>vector&lt;int&gt; ve[N]</code>，但是下标很大，又不想离散化时可以使用。</p>',code:{label:"大下标数组",text:["map<int, vector<int>> mp;","mp[1000000000].push_back(1);"].join("\n")}},{tag:"18 · 习题",title:"习题",body:'<div class="ex-table">  <table>    <thead><tr><th>题号</th><th>链接</th><th>备注</th><th>难度</th></tr></thead>    <tbody>      <tr><td class="mono">P1918</td><td><a href="https://www.luogu.com.cn/problem/P1918" target="_blank" rel="noopener">保龄球</a></td><td>数字映射</td><td>橙题</td></tr>      <tr><td class="mono">P3405</td><td><a href="https://www.luogu.com.cn/problem/P3405" target="_blank" rel="noopener">Cities and States S</a></td><td>满足要求的对数</td><td>橙题</td></tr>      <tr><td class="mono">P3613</td><td><a href="https://www.luogu.com.cn/problem/P3613" target="_blank" rel="noopener">寄包柜</a></td><td>map套map</td><td>橙题</td></tr>      <tr><td class="mono">P5266</td><td><a href="https://www.luogu.com.cn/problem/P5266" target="_blank" rel="noopener">学籍管理</a></td><td>名字映射分数</td><td>黄题</td></tr>      <tr><td class="mono">P3879</td><td><a href="https://www.luogu.com.cn/problem/P3879" target="_blank" rel="noopener">阅读理解</a></td><td><code>map&lt;string,vector&lt;int&gt;&gt;</code></td><td>橙题</td></tr>      <tr><td class="mono">P1102</td><td><a href="https://www.luogu.com.cn/problem/P1102" target="_blank" rel="noopener">A-B 数对</a></td><td></td><td>橙题</td></tr>      <tr><td class="mono">arc061_b</td><td><a href="https://atcoder.jp/contests/abc045/tasks/arc061_b" target="_blank" rel="noopener">Snuke\'s Coloring</a></td><td>3* 3全都包含指定坐标</td><td>1682/橙题</td></tr>      <tr><td class="mono">P2671</td><td><a href="https://www.luogu.com.cn/problem/P2671" target="_blank" rel="noopener">求和</a></td><td><code>map&lt;string,vector&lt;int&gt;&gt;</code></td><td>绿题</td></tr>      <tr><td class="mono">CF670C</td><td><a href="https://codeforces.com/problemset/problem/670/C" target="_blank" rel="noopener">Cinema</a></td><td></td><td>1300/橙题</td></tr>      <tr><td class="mono">Yosupo</td><td><a href="https://judge.yosupo.jp/problem/associative_array" target="_blank" rel="noopener">Associative Array</a></td><td>大下标</td><td></td></tr>    </tbody>  </table></div>'}]}}();